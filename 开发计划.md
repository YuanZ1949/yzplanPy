# YZplan 开发计划

## 一、需求分析

| # | 需求 | 说明 |
|---|------|------|
| 1 | 主窗口 + 托盘 | 可隐藏主界面，系统托盘菜单常驻 |
| 2 | 程序设置 | 开机自启开关、各模块开关 |
| 3 | 四个选项卡 | 主页（组件式/可拖拽）、模块、程序设置、关于 |
| 4 | 路径传递模块 | Ctrl+G 全局快捷键：把最后一个资源管理器地址填入当前聚焦的文件对话框 |
| 5 | 配置信息模块 | 读取电脑硬件/系统配置信息 |
| 6 | RSS 聚合模块 | 多源抓取、合并去重、同一内容多来源标签标注 |

现有项目基本为空壳（`Modules/` 下已有 `bulletin/events/gui/monitor/MyAnime/rss/timeline` 占位文件，还有 `interface/`、`translation/` 目录、`config.xml`），可复用改名。

## 二、技术选型（已确认）

- **GUI**：PySide6 (Qt6)
- **Python**：3.11
- **路径传递**：仅 Windows（COM + SendInput 方式）
- **存储**：SQLite（RSS 数据）+ JSON（程序配置）
- **打包**：PyInstaller（onedir，--noconsole）

主要第三方依赖：`PySide6`、`feedparser`、`requests`、`psutil`、`pywin32`（可选，路径模块用）、`pytest`（测试）。

## 三、架构设计

```
yzplanPy/
├─ main.py                  # 入口：单实例检查 → App → 主窗口
├─ core/
│  ├─ app.py                # QApplication 封装、全局注册
│  ├─ config.py             # JSON 配置读写、热重载
│  ├─ hotkey.py             # 全局快捷键（WM_HOTKEY 原生事件过滤器）
│  ├─ tray.py               # QSystemTrayIcon + 托盘菜单
│  ├─ autostart.py          # 注册表 Run 键写入/删除
│  ├─ singleinstance.py     # QLockFile 防止多实例
│  └─ native_event.py       # QAbstractNativeEventFilter 处理 WM_HOTKEY
├─ ui/
│  ├─ mainwindow.py         # 主窗口，四个选项卡容器
│  ├─ home_tab.py           # 主页：QGraphicsView 可拖拽/缩放组件画布
│  ├─ widget_item.py        # 主页组件（模块入口卡片/信息卡片/命令按钮）
│  ├─ modules_tab.py        # 模块列表：开关 + 设置按钮
│  ├─ settings_tab.py       # 程序设置：自启、行为选项
│  └─ about_tab.py          # 版本、更新检查、开发者信息
├─ modules/                 # 插件式模块（既有目录复用）
│  ├─ base.py               # 模块基类：元信息、启停、设置 schema
│  ├─ registry.py           # 模块注册表：扫描、启用状态、加载
│  ├─ path_forward.py       # 需求4：路径传递
│  ├─ sys_info.py           # 需求5：配置信息
│  ├─ rss_aggregator.py     # 需求6：RSS 聚合
│  ├─ MyAnime.py / rss.py   # 既有占位，逐步迁移
│  └─ bulletin.py / events.py / monitor.py / timeline.py / gui.py
├─ data/
│  ├─ settings.json         # 用户配置
│  └─ app.db                # SQLite：RSS 表、去重、历史
├─ translation/             # Qt Linguist .ts/.qm 国际化
├─ tests/                   # pytest 测试
├─ build/                   # PyInstaller spec 与产物
└─ 开发计划.md
```

**模块注册表机制**：每个模块一个包，暴露 `MODULE_INFO = {id, name, description, ...}` 与 `Module` 类（`start()/stop()/create_widget()`）。`registry.py` 扫描 `Modules/`，模块选项卡据此生成开关和设置页；主页可引用任意已启用模块的组件。

## 四、三大模块技术路线

**4. 路径传递（path_forward）**
1. 捕获：`Shell.Application` COM 枚举 `Shell.Windows()`，取最近活动的资源管理器窗口 → `LocationURL`（file:///）→ 解码为本地路径，存入最近路径缓存。
2. 触发：全局 Ctrl+G。用 Win32 `RegisterHotKey` + Qt `QAbstractNativeEventFilter`（无需管理员、不依赖第三方 hook 库）。
3. 注入：将目标对话框/资源管理器置前 → 路径写入剪贴板 → `keybd_event` 发送 `Ctrl+V` → `Enter`（对 Open/保存对话框的"文件名"输入框均有效）。
4. 回退方案：若注入失败，用 `pywinauto`(UIA) 定位 `FileNameControlHost` 控件精确填写。

**5. 配置信息（sys_info）**
- OS 版本 (`platform`) / CPU 型号与核心数 (`platform`+`psutil`) / 内存 (`psutil`) / 磁盘 (`psutil`) / GPU (`winreg`) / 网络适配器（可选）。提供只读信息卡片组件。

**6. RSS 聚合（rss_aggregator）**
1. 抓取：`feedparser` 解析多源（RSS/Atom），`requests` 拉取。
2. 去重：规范化 `(title, link)` 哈希，SQLite 唯一索引；重复条目只收一次。
3. 标签标注：同一条目被多个来源报道时，合并展示并标记来源标签（如 `[站点A][站点B]`）。
4. 后台定时刷新（QTimer + QThread 防阻塞），主页提供聚合信息卡片/列表组件。
5. 数据模型：`feeds`（源）、`items`（条目）、`item_sources`（条目-来源多对多）。

## 五、开发路线图

**Phase 0 工程骨架（半天）**
- 修复 git 所有权问题（`safe.directory`），建立 venv（Python 3.11）
- 安装依赖，`main.py` 可启动空白主窗口 + 系统托盘 + 隐藏/显示
- JSON 配置系统 + 单实例锁

**Phase 1 核心框架（1 天）**
- 四个选项卡布局（主页/模块/设置/关于）
- 模块注册表（扫描、启用存储、设置 schema）
- 开机自启（注册表 Run 写入/删除）
- 全局快捷键框架（RegisterHotKey + native event filter）

**Phase 2 三大功能模块（2 天）**
- `path_forward`：路径捕获 → 注入全链路，Windows 实测调优
- `sys_info`：信息采集 + 信息卡片组件
- `rss_aggregator`：多源抓取、去重、来源标签、定时刷新、SQLite 持久化

**Phase 3 主页组件式画布（1 天）**
- QGraphicsView 画布：组件拖拽移动、缩放、布局保存到 JSON
- 预制组件：模块启动卡片、系统信息卡片、RSS 列表卡片、命令按钮

**Phase 4 完善与发布（1 天）**
- 关于页：版本号、更新检查（预留 API）、开发者信息
- `translation/` 国际化接入（可选）
- PyInstaller 打包为无控制台 onedir exe，冒烟测试

## 六、测试与质量
- `pytest`：RSS 去重/标签合并、URL→路径解码、配置读写
- 手工冒烟：托盘、快捷键注入、自启写入/删除
- 每次改动后 `python -m compileall` 与模块级单测

## 七、风险与对策
| 风险 | 对策 |
|------|------|
| 快捷键注入被部分对话框拦截 | SendInput 方案 + pywinauto 兜底，失败时提示手动粘贴 |
| psutil 在 Windows 上读 GPU 不可靠 | GPU 用注册表查询，纯 Win32 无重依赖 |
| PySide6 打包体积大 | onedir + exclude 无用模块，资源用 qrc 编译进 exe |
| 网络请求阻塞 UI | RSS 抓取一律后台线程 + 队列回传 |

## 八、实现状态与运行方式（2026-08-24）

**当前进度**：Phase 0–3 已完成，Phase 4 已完成（翻译钩子、PyInstaller spec 与构建、pytest 冒烟）。

**目录结构与约定**
- `main.py`：入口。`core/qt_bootstrap.py` 处理 Windows 系统 ICU（`icuuc.dll` 隐式加载 WinError 127）问题的预加载兜底。
- `modules/`：插件式模块注册表，现注册 `path_forward`、`sys_info`、`rss_aggregator`。
- `ui/home_tab.py`：主页 QGraphicsView 画布，组件可拖拽/拖角缩放/删除，布局持久化到 `data/settings.json` 的 `home.layout`。
- `data/`：`settings.json`（配置）与 `app.db`（RSS 库），运行时自动创建。
- 旧占位 `Modules/...` 已迁移/跳过，见 `modules/registry.py` 的 `_SKIP`。

**依赖与运行**
- 本项目网络盘（`\\yuannas\mnt\HDD4T1\...`）SMB 不稳定，venv 放在本地磁盘：
  `C:\Users\yuan\AppData\Local\Temp\opencode\yzplan-venv`
- 因 Windows 自带 ICU Bug，在 venv `Lib\site-packages\sitecustomize.py` 中预加载 ICU，使普通导入与 PyInstaller 分析子进程均可 import PySide6。
- 运行：`..\yzplan-venv\Scripts\python.exe main.py`（直接双击开发运行）
- 测试：`..\yzplan-venv\Scripts\python.exe -m pytest tests`
- 冒烟：`..\yzplan-venv\Scripts\python.exe smoke_test.py`

**打包**
- spec：`build/yzplan.spec`（onedir，`--noconfirm --clean --distpath` 指向本地盘）。
- 构建命令示例：
  `python.exe -m PyInstaller --noconfirm --clean --distpath C:\Users\yuan\AppData\Local\Temp\opencode\dist --workpath ...\pybuild build\yzplan.spec`
- 产物：`dist\YZplan\YZplan.exe`（含 `_internal`，已含 Qt6 库、qwindows 平台插件与 conda 的 sqlite3/openssl 等 DLL）。
- 后续优化：用 python.org 官方 Python 替代 anaconda 可让打包更干净；exe 运行验证需在有桌面的会话中手动冒烟（托盘、Ctrl+G、自启）。