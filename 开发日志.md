# YZplan 开发日志

> 记录 2026-08-25 进行的打包与环境修复、UI 与托盘退出问题修复、Fluent 风格 UI 转型。

---

## 2026-08-25

### 一、环境与打包

**背景**：原有 `venv` 可复用（Python 3.11.7 + PySide6 6.11.2），但 `pip` 缺失，先补装。

1. **补装 pip / 依赖**
   - `python -m ensurepip --default-pip` 补装 pip（venv 早期未装）。
   - 发现 `psutil`、`feedparser`、`charset_normalizer` 等依赖实际未安装，执行 `pip install -r requirements.txt` 补齐。

2. **PyInstaller 打包（onedir）**
   - 原 `build/yzplan.spec` 指向失效路径（`Y:\...`、conda DLL），已废弃；根目录新建 `yzplan.spec`。
   - 打包反复踩坑，逐一定位：
     - **Qt 平台插件缺失**：`hook-PySide6.py` 因 QtLibraryInfo 隔离子进程无法导入 QtCore 而跳过主体，plugins 未自动收集。解决：spec 中手动把 `PySide6\plugins` 加入 `datas`，并加入 `qt.conf` + 设置 `QT_PLUGIN_PATH`（运行时 hook）。
     - **Python DLL 加载失败 / conda 运行库缺失**：当前 venv 基于 anaconda3 python311，打包依赖其基础解释器。解决：显式打包 `sqlite3.dll`、`libcrypto-3-x64.dll`、`libssl-3-x64.dll`、`liblzma.dll`、`LIBBZ2.dll`、`ffi.dll`。
     - **动态模块未被分析**：`modules/` 子模块由 `registry.py` 运行时动态导入，PyInstaller 静态分析不到。解决：`modules.path_forward`、`modules.sys_info`、`modules.rss_aggregator` 加入 `hiddenimports`，并补 `ctypes.wintypes`。
     - **`platform` / `psutil` 等导入缺失**：依赖被动态导入链切断，逐个补 `hiddenimports`。
   - **产物**：`dist\YZplan\YZplan.exe`（onedir，含 `_internal/` 与 Qt 插件），已在无 anaconda PATH 的干净环境验证启动正常。
   - **再次打包易错提醒**：`qt.conf` 是 spec 引用的数据源，删掉会报 `Unable to find qt.conf`，需保留。

### 二、代码修复

1. **`core/constants.py` — Frozen 路径**
   - 新增 `sys.frozen` 分支：打包后 `PROJECT_DIR`/`DATA_DIR` 改为 exe 同级目录，`data/`（settings.json、app.db）落到 `dist\YZplan\data\`，开发状态下保持原逻辑。

2. **`modules/registry.py` — 模块扫描**
   - 原 `os.listdir(MODULES_DIR)` 在打包后目录不存在（PYZ 存档），改为 `pkgutil.iter_modules()`，开发/打包均可用；`_SKIP` 改为无后缀模块名。

3. **`ui/home_tab.py` — 主页重构（消除重叠、可排序）**
   - 原 QGraphicsView 自由画布默认位置偏移过小（30~40px vs 340×230 卡片），首启即大量重叠。
   - 重写为**纵向卡片列表**：卡片永不重叠；每张卡片标题栏提供 `▲ 上移` / `▼ 下移` / `✕ 移除`；顶部保留 `添加`、`恢复默认顺序`、`清空`。
   - 顺序持久化到 `data/settings.json` 的 `home.order`；兼容旧版 `home.layout`（按 Y 坐标转初始顺序）。

4. **`ui/mainwindow.py` — 托盘退出无法关闭程序**
   - 根因一：`main.py` 设置了 `setQuitOnLastWindowClosed(False)`，关闭窗口不会结束事件循环，程序常驻托盘。
     - 修复：`quit()` 中调用 `self.context.app.quit()` 真正退出。
   - 根因二：首次修复误用 `QtWidgets.QApplication.quit()`，但 `QtWidgets` 是 `__init__` 局部变量，方法内未定义 → `NameError`。
     - 改用 context 中的 QApplication 实例，重新打包后验证托盘退出正常。

### 三、Fluent 风格 UI 转型（PyQt-Fluent-Widgets / PySide6 分支）

- **安装**：`PySide6-Fluent-Widgets 1.11.3`（import 名 `qfluentwidgets`），依赖 `PySideSix-Frameless-Window`、`darkdetect`，已加入 `requirements.txt`。
- **ICU 预载前移**：qfluentwidgets 顶层直接 `from PySide6... import`，必须在任何 Qt 导入前预载 ICU（WinError 127）。`core/qt_bootstrap.py` 增加模块级 `_preload_icu()`（仅 win32）。
- **UI 改造**：
  - `ui/mainwindow.py`：`QTabWidget` → `FluentWindow` + 左侧导航栏（主页/模块/程序设置/关于），沿用 close_to_tray 与 quit 逻辑。
  - `ui/home_tab.py`：卡片改用 `CardWidget`，按钮 `PrimaryPushButton/PushButton/TransparentToolButton`，排序/移除逻辑不变。
  - `ui/modules_tab.py`：模块行改用 `CardWidget` + `SwitchButton`（开/关）+ `PushButton`。
  - `ui/settings_tab.py`：设置项迁移为卡片内 `SwitchButton` 行（开机自启/关闭驻留/启动隐藏）。
  - `ui/about_tab.py`：`SubtitleLabel/BodyLabel/PrimaryPushButton` 排版。
- **打包注意**：qfluentwidgets 资源内置在 `_rc/resource.py`，PyInstaller 直接编入 PYZ；其依赖的 `QtSvg/QtXml/QtSvgWidgets` 已被自动收集，无需额外 hiddenimports。构建完成后干净 PATH 环境验证 exe 启动正常。

### 四、Fluent 主题修复（字体警告 + 黑白不统一）

1. **QFont::setPointSize: Point size <= 0 (-1) 警告**
   - 根因：qfluentwidgets 在 `import` 时注册图标字体（`QFontDatabase.addApplicationFontFromData`），而 main.py 原先**先导入 ui 模块再创建 QApplication**，字体库未就绪导致 pointSize=-1。
   - 修复：把 qfluentwidgets 涉及的 import（`ui.mainwindow`、各 tab、`core.theme`）延迟到 `QApplication` 创建之后；同时 `app.setFont(QFont("Microsoft YaHei", 9))` + 设置 HighDpi 舍入策略。

2. **浅色/深色主题不统一**
   - 根因：qfluentwidgets 主题跟随系统（darkdetect），导航/卡片为深色，但内容区普通 QWidget 用 Qt 默认浅色调色板 → 黑白混杂。
   - 修复：新增 `core/theme.py` 统一调控——`setTheme` 控制 qfluentwidgets；深色时用 `QPalette` 给普通控件设深色底/浅色字，浅色时重置为 standardPalette；切换后全量 `allWidgets().update()`。
   - 设置项：`ui/settings_tab.py` 增加「界面主题」下拉（跟随系统 / 浅色 / 深色），即时保存 `ui.theme` 并生效；`constants.py` 默认 `ui.theme=auto`；启动时 main.py 读取并应用。
   - `smoke_test.py` 同步调整（先建 QApplication 再导入 ui；`.tabs.count()` → `stackedWidget.count()`）。

### 五、Ctrl+G 失灵修复与主页/窗口缩放

1. **Ctrl+G 快捷键彻底修复（此前一直无效）**
   - 根因一：`HotKeyFilter` 未继承 `QAbstractNativeEventFilter`，`app.installNativeEventFilter(self)` 抛 `TypeError`，而 `path_forward.start()` 里 `except Exception` 把它静默吞掉 → 快捷键从未注册。
   - 根因二：PySide6 虚函数签名是 `nativeEventFilter(self, event_type, message, result=None)`（`result` 为可选），之前下划线名+三参写法均不匹配。
   - 修复：`core/hotkey.py` 改为 `class HotKeyFilter(QtCore.QAbstractNativeEventFilter)`，`super().__init__()`，方法名 `nativeEventFilter`、`result` 缺省；并提高 hotkey_id 防冲突。已用模拟 Ctrl+G 验证回调触发；路径捕获与 URL→本地路径解码均正常。
2. **组件颜色与主窗口不匹配**
   - 主页卡片由 QFrame（默认调色板）改回 `CardWidget`（Fluent 主题卡片底色，与模块页/设置页同款），标题 `StrongBodyLabel`、关闭钮 `TransparentToolButton`；画布背景 `palette(window)` 随主题。
3. **右下角同时调整宽高**
   - 手柄加大到 20px（更易抓取）；模拟拖拽验证：右下角手柄拖 (180,130) → 宽高同时 +180/+130；右边柄只改宽、下边柄只改高。
   - 主程序窗口：qframelesswindow 默认 `_isResizeEnabled=True`，WM_NCHITTEST 已含 `HTBOTTOMRIGHT`（右下角双向缩放）。

### 六、今天的产物与验证

- 打包配置：`yzplan.spec`（根目录）
- 运行：`dist\YZplan\YZplan.exe`
- 数据目录：`dist\YZplan\data\`（运行时自动创建 settings.json、app.db）
- 测试：`pytest tests` 全部通过（8 passed）——进程尾部 0xc0000139 为 anaconda+Qt 的既有问题，不影响打包产物。
- 说明：`build\yzplan\yzplan\YZplan.exe` 是 PyInstaller 中间产物，**不可直接运行**，应运行 `dist\YZplan\YZplan.exe`。

---

## 2026-08-29

### 一、主页组件：外框拉伸不同步 + 拖拽换序失效

1. **横向拉伸时卡片外框不同步（内容被裁剪）**
   - 复现确认：Qt 6.11.2 的 `QGraphicsProxyWidget` 中，创建时调用 `setFixedWidth` 会“毒化”宽度传播——之后用 `setFixedSize` 改宽度时，内层 QWidget 变了、proxy 外框不变（垂直拉伸后裁剪区域才显现）。`setMinimumWidth`/`setMinimumSize`/`setFixedHeight` 均不触发此问题，仅 `setFixedWidth` 触发。
   - 修复（`ui/home_tab.py`）：
     - 创建卡片 `card.setFixedWidth(card_w)` → `card.resize(card_w, saved.get("height", _DEF_H))`
     - 手柄 r/c 拖拽改宽 `self._card.setFixedWidth(new_w)` → `self._card.setFixedSize(new_w, self._card.height())`
   - 验证：流式拉伸与手柄拖拽后 proxy 外框宽度均与内层内容一致。

2. **拖动换序完全失效（用户反馈“不太支持拖动换位置”）**
   - 根因：`_Proxy.mouseMoveEvent` 用 `event.button() == QtCore.Qt.NoButton` 判断是否按住左键，但 Qt 中**鼠标移动事件的 `button()` 恒为 `NoButton`**（按住的键在 `buttons()` 里）→ 拖动永远不越过 `_DRAG_THRESHOLD`。
   - 单元测试此前手造事件把 `button` 设为 `LeftButton`（现实中不存在），误成“看似通过”。
   - 修复：改用 `event.buttons() == QtCore.Qt.NoButton`（`ui/home_tab.py`）；测试改为真实语义（`setButton(NoButton)` + `setButtons(LeftButton)`），新增回归用例 `test_proxy_move_event_real_button_state`。
   - 说明：卡片右上角隐藏的 ✕ 是「移除」按钮，非「位置切换」；换序即通过拖动完成。

### 二、RSS 模块两处运行异常

1. **编辑订阅源崩溃**：`_EditFeedDialog(self.owner, feed, self)` 参数顺序错误（`__init__(self, feed, store, parent)` 期望 feed 在前），Module 对象被当作 feed → `feed.get("name")` AttributeError。改为 `_EditFeedDialog(feed, self.owner.store, self)`（对齐 1618 行页面入口写法）。
2. **自动刷新访问已销毁控件崩溃**：`Module._widgets` 只在 `create_home_widget` 追加、从不清理；主页控件销毁后自动刷新仍回调其 `on_refreshed` → `RuntimeError: Internal C++ object (QLineEdit) already deleted`。
   - 修复：新增 `_forget_widget(w)`（按 `x is w` 身份删除）；`create_home_widget` 连接 `w.destroyed` 自动清理；`_on_refreshed` 遍历副本并对 `RuntimeError` 兜底。

### 三、主窗口几何：首启自适应尺寸 + 恢复不生效

1. **首启默认尺寸改为按屏幕自适应（16:10）**
   - 首次启动（`ui_state` 无记录）不再用固定 960×640：宽度取屏幕可用区 80%，高度按 16:10 换算；若高度超出可用区 82%，改为以高度反推宽度；整体不超过可用区并**居中**。`core/constants.py` 默认同步为 1280×800。

2. **`ui_state.apply()` 位置恢复从未生效**
   - 根因：`QGuiApplication.screenAt(QRect(...))` 在 PySide6 6.11 **只接受 `QPoint`** → 每次恢复都抛 TypeError，且被 `except` 静默吞掉、照样返回 True。窗口尺寸能还原（resize 在异常之前），但 `move(x,y)` 永远执行不到。
   - 修复：`screenAt(QPoint(x + w//2, y + h//2))`；`window_geometry()` 挂上 `ui` logger；恢复失败记日志并返回 False（不再假装成功）。

3. **“默认小窗口”真凶：qframelesswindow 原生层缩窗**
   - 实测：`start_hidden=True`（配置中已开启）时窗口创建后**未显示**，事件循环跑一轮后 FluentWindow 被原生层缩回初始 500×500（跨 C++ 侧，Python hook 不到），从托盘打开时即为此前常见的“小窗口”。
   - 修复（`ui/mainwindow.py`）：几何恢复抽成 `_apply_geometry()`，构造时应用一次；**首次 `showEvent` 同步再应用一次**，并补 0ms/60ms 两个延迟兜底重放，确保保存的尺寸+位置最终生效。
   - 验证（真实桌面非 offscreen）：构造后 1202×791@(150,50) → 隐藏缩到 500×500 → 首次显示立即恢复并稳定在 1202×791@(150,50)。

### 四、测试与杂项

- 全量 `pytest` 51 个用例通过（含主页拖动回归、RSS 生命周期验证）。
- `.gitignore` 补充 SQLite WAL/SHM/Journal（`data/*.db-shm`、`*.db-wal`、`*.db-journal`）与单实例锁 `data/yzplan.lock`。
- 修复版本提交：`8e3c129`。

---

## 2026-08-30

### 一、RSS 聚合模块：分组头风格对齐普通订阅源（`modules/rss_aggregator.py`）

1. **`_HeadRow.setStyleSheet` 选择器物配修复**
   - 原逻辑直接把 `QPushButton#...` 的 CSS 规则赋给 `QLabel`（`QPushButton` 选择器不匹配 QLabel，导致样式**从未生效**），且多条规则互相覆盖。
   - 修复：`_HeadRow`（与 `_WrapRow`/`_AutoRow` 一致）先把 `QPushButton`→`QLabel`，再按 objectName（`rssHeadTitle`/`rssHeadCount`）分派，并把基础规则与 `:hover` 规则合并到同一串，一并赋给整个 head。
   - 离屏渲染确认：head 标题样式现为 `QLabel#rssHeadTitle{color:palette(text)...} + :hover{color:#1976d2}`，来源徽标为 `#e8f0fe/#1967d2` 浅蓝胶囊、`border-radius:3px`、`font-size:12px`。

2. **标题去粗体、字号对齐普通条目**
   - 原 `_HeadRow` 标题 `f.setBold(True)` + QSS `font-weight:bold`，比普通订阅条目（`_WrapRow` 11pt 非粗体）更粗更"大"。
   - 移除 `f.setBold(True)`（`_HeadRow.__init__`）与 QSS 中的 `font-weight:bold`，保留 `_TITLE_FONT_PX`（11pt）——与普通条目 `_WrapRow` 完全一致（head `bold=False, size=11.0` vs wraprow `bold=False, size=11.0`）。
   - 聚合成员行本就与普通行共用 `_make_item_row`（非粗体 11pt），无需改动。

3. **保留三交互**（连接逻辑未动）
   - 单击标题 = 预览该分组（`titleClicked`→`_agg_head_preview`）
   - 双击标题 = 默认打开一个来源（`headDoubleClicked`→`_agg_head_open`）
   - 单击来源徽标 = 展开/折叠全部来源（`badgeClicked`→`_toggle_torrent_group`）
   - 徽标文案为 `"N 来源"`（去掉旧 "N 条"）。

### 二、RSS 设置页米黄面板 → 壁纸透出一致性（`_SettingsDialog`）

- **根因**：`QScrollArea` 内容 `QWidget` 的 `autoFillBackground=True` + `#efefef` 整块盖住对话框 `paintEvent` 绘制的壁纸；且只调 `setAutoFillBackground(False)` 无效（被 QSS 重新置 True）。
- **修复**：scroll `viewport()` 与 `content` 均 `setAutoFillBackground(False)`，`content` 设 `objectName("rssSettingsContent")` 并 `QWidget#rssSettingsContent{background:transparent}`——用 objectName 限定透明，避免宽泛 `QWidget{background:transparent}` 破坏 `QLineEdit`/`QSpinBox` 输入框底。
- **验证**：离屏渲染设置对话框中心像素由米黄 `239,239,239` 变为壁纸色 `(350,180)=198,218,225` / `(350,350)=143,144,203`。
- **范围**：按用户确认，仅改设置页 `_SettingsDialog`；分类/关键词/条目预览等小窗口不贴壁纸，保持原样。

### 三、RSS 模块页面壁纸透出确认

- RSS 页嵌入"会画壁纸的窗口"时，列表区/侧边栏/工具栏/预览区容器本就透明，均可透出壁纸色，无需额外改动容器。

### 四、聚合条目多选支持（`modules/rss_aggregator.py`）

1. **`_HeadRow` 新增复选框**
   - `_HeadRow.__init__` 增加 `QCheckBox` + `checkboxToggled` 信号 + `set_check_state`/`check_state` 方法，布局顺序：checkbox → title_label → count_label。
   - 新增 `_on_head_checkbox_toggled(head_hash, checked)`：勾选 head → 勾选/取消该组所有成员条目的 checkbox；取消 → 从 `_selected_hashes` 移除。
   - 新增 `_sync_head_checkbox_state(head_hash)`：遍历成员 checkbox 状态，按全选/部分选/未选设置 head 的 `Checked`/`PartiallyChecked`/`Unchecked`。

2. **`_load_torrent_aggregation` 连接**
   - head 连接 `checkboxToggled` → `_on_head_checkbox_toggled`
   - 每个成员 checkbox 连接 `toggled` → `_sync_head_checkbox_state(head_hash)`
   - 建立 `_head_by_member` 映射（member_hash → head_hash）；加载后初始化每个 head 的 checkbox 状态。

3. **全选联动**
   - `_select_all` 末尾遍历 `_group_children` 调用 `_sync_head_checkbox_state`，确保"全选"/"取消全选"后 head checkbox 状态同步。

### 五、聚合条目换行截断修复

1. **`_HeadRow.heightForWidth` 漏算 checkbox 宽度**
   - 加入 checkbox 后，可用宽度需额外减去 `checkbox.sizeHint().width() + spacing`。原来只减 `count_label + spacing`，导致标题按较宽计算高度 → 实际渲染被 checkbox 挤窄 → 换行更多 → 高度不够 → 截断。
   - 修复：`spawn` 增加 `checkbox.sizeHint().width() + self.layout().spacing()`。

2. **`_toggle_torrent_group` 展开后未重算行高**
   - 展开/折叠后成员行从隐藏变可见，但 `sizeHint` 沿用旧值。
   - 修复：`_toggle_torrent_group` 末尾加 `QTimer.singleShot(0, self._sync_row_heights)` 重新计算。

### 六、聚合条目标题截断与风格对齐

1. **标题数据级截断**
   - 根因：`head_title = (g.get("title") or "(无标题)")[:60]` 在数据层截断到60字符，`[:120]` 无效（已被截断）。
   - 修复：去掉 `[:60]` 和 `[:120]`，标题完整传入 `_HeadRow`，由 word-wrap 自动换行。

2. **行距/字距对齐**
   - `_HeadRow` 标题 QSS padding：`6px 4px` → `2px`（与普通条目 `_WrapRow` 一致）。
   - `_HeadRow` 布局 spacing：`8` → `6`（与 `_AutoRow` 一致）。
   - 离屏验证：head SS 已变为 `padding:2px`，spacing=6，与普通条目完全一致。

### 七、回归与测试

- 更新 `tests/test_rss_sidebar.py::test_agg_head_count_at_end_no_newline` 以匹配新徽标文案 `"N 来源"`。
- 全量 `pytest tests/` 115 个用例通过 + `smoke_test.py` SMOKE OK。

---

## 2026-08-30（晚间 · RSS 四项收尾）

> 本会话针对 RSS 模块完成：选择功能根因修复、条目列表去图片、订阅源多标签（改数据库）、预览摘要。改动文件：`modules/rss_aggregator.py`、`modules/rss_store.py`、`core/constants.py`。

### 一、选择相关功能失效——根因修复（`rss_aggregator.py`）

**现象**：全选（全选/标记已读/未读/删除选中）点了没反应；单条目勾选却正常。

**根因**：PySide6 中 `QCheckBox.stateChanged` 发出的是**普通 int**（未勾选 `0`、勾选 `2`），而代码里写成 `checked = state == QtCore.Qt.Checked`。经实测 `2 == QtCore.Qt.Checked` 恒为 `False`（int 与 `CheckState` 枚举在 PySide6 中不相等）→ 全选永远走「取消选择」分支，把 `_selected_hashes` 清空。此前诊断脚本直接调 `_select_all(QtCore.Qt.Checked)` 传的是枚举，误测为「逻辑正确」。

**修复**：比较改为 `state == QtCore.Qt.CheckState.Checked.value`（`rss_aggregator.py` 全选），并排查出**同源隐患**两处同步修正：
- `chk_notify.stateChanged` 两处 lambda（1413/2831）同型比较。
- 握手验证：单勾选→全选→取消全选→批量已读，`_selected_hashes` / 复选框 / 未读数均正确。

### 二、条目列表不再展示图片（`rss_aggregator.py`）

- 删除 `_make_item_row` 中的缩略图渲染块（`show_thumbnail and it.get("image_url")`）。
- 删除「显示缩略图」切换按钮 `chk_thumbnail` 与 `_toggle_thumbnails` / `_update_thumbnail_btn_text` / `_show_thumbnails` 状态。
- 删除随之失联的异步缩略图工具：`_load_thumb_async`、`_get_network_mgr`、`_network_mgr`、`_widget_alive`。
- `_make_item_row` 精简签名（去掉 `show_thumbnail` 形参），两处调用与 `core/constants.py` 的 `rss.show_thumbnails` 默认键一并移除。

### 三、订阅源支持多标签（改数据库，`rss_store.py` + 对话框）

按用户确认采用「一源多标签」方案，正文模型 `item_sources(hash,tag)` 本就支持一物多标签，限制在**源端**（`feeds.tag` 单列）。

1. **数据层（`rss_store.py`）**
   - 新增 `feed_tags(feed_id, tag)` 表（`_init_schema` 内 `CREATE TABLE IF NOT EXISTS`，兼容既有库）。
   - `add_feed` 接受 `tags` 列表，同事务写入 `feed_tags` 并回填 `feeds.tag`（取首标签作旧版兼容）。
   - `get_feed_tags` / `set_feed_tags` 存取多标签；`get_feed_by_id`、`list_feeds`、`search_feeds`、`list_sidebar` 均附带 `tags` 列表。
   - `update_feed` 拦截 `tags` 键：修复“仅改标签时 `updates` 为空提前 return”导致标签不落库的 bug。
   - `ingest` 入参兼容字符串/列表，把订阅源**全部标签**写入 `item_sources`（每条一标签多行）；`remove_feed` 顺带清理 `feed_tags`。

2. **抓取（`rss_aggregator.py`）**
   - 新增 `_final_tags(f)`（缓存 `f["tags"]`，否则查 `get_feed_tags`，再回退 `[f["tag"] or f["name"]]`）；`_process_feed` 入库改为传标签列表；`_final_tag` 取首标签。

3. **对话框**
   - 添加/编辑订阅源输入框 placeholder 改为「多个标签用逗号分隔」，保存时按 `，`/`,` 拆分；编辑框回填时用 `feed["tags"]` 以逗号拼接显示。
   - 源管理列表标签展示升级为多标签 `(标签: A, B)`。

4. **联动**：`_load_tag_filter` 读取 `item_sources.tag`，多标签入库后自然出现在标签筛选下拉；侧边栏按 `feed_id`（`item_feeds`）过滤，与标签无关，无需改。

### 四、预览区上方新增摘要（`rss_aggregator.py`）

- 预览列外包 `_preview_container`，顶部放置 `_summary_title`（标题，粗体 15px）、`_summary_meta`（标签/更新时间）、`_summary_desc`（去标签纯文本描述，限制 120 高 / 400 字截断），下方才是网页/文本预览。
- 新增 `_set_summary(item_data)`，在 `_display_preview` 开头调用填充。
- 样式不再依赖 `pc` 的 `fg` 键（原 `pc` 仅 panel/panel_soft/border，直接 `.format(**pc)` 曾抛 `KeyError: 'fg'`，首轮全量测试失败后改为不加颜色的透明背景文本）。

### 五、异步竞态崩溃修复（附）

- 修复 `libshiboken: Internal C++ object (QListWidgetItem) already deleted`：`_sync_head_checkbox_state` 遍历 `_group_children` 时，列表重建后条目已销毁。加 try/except `RuntimeError` 跳过失效项，并以有效项数重算 `total`，避免全选/联动时崩溃。

### 六、回归与验证

- 多标签端到端（独立 temp DB）：`add_feed(tags=[...])` → `get_feed_by_id(tags)` / `list_feeds` / `list_sidebar` 均含多标签；`ingest` 后 `get_item.tags` 为 `A | B | C`；`update_feed` 仅改标签也能落库。
- 全量 `pytest tests/`（115 用例，含既有 RSS 侧边栏/聚合/选择）全部通过，`ast` 语法检查 OK。

---

## 2026-08-31（构建环境去 anaconda 依赖 + README 整理）

### 一、build.bat：构建前后备份/恢复运行时数据

- 目的：构建（PyInstaller onedir）会重建 `dist\YZplan\`，若 `data/` 位于被覆盖路径下，运行生成的数据可能被清掉。
- 新增逻辑（`build.bat`）：
  - 构建前把 `data/app.db`、`data/settings.json`、`data/logs/` 移到临时目录 `_data_bak_%RANDOM%`；
  - 构建完成后移回（logs 用 `xcopy` 合并，避免目标目录已存在），最后清理临时目录；
  - 临时目录名带随机数防冲突；即使构建中断，数据也只是暂存不丢失。

### 二、yzplan.spec：解耦 anaconda / 自动适配标准 CPython venv

**背景与根因**：
- 冻结产物报 `ImportError: DLL load failed while importing _ctypes`。
- 根因：当前 `.venv` 基于 anaconda3 python311，编译扩展模块（`_ctypes.pyd`、`_socket.pyd`、`_ssl.pyd`、`_sqlite3.pyd` 等）与 `sqlite3.dll`、`libssl/libcrypto`、`ffi.dll` 等**都位于 base conda 根目录（venv 之外）**，PyInstaller 不会自动收集 → exe 启动即崩。
- 旧 spec 的 conda DLL 路径默认指向 `~/anaconda3`（`C:\Users\yuan` 下不存在）→ 这些 DLL 其实没被打进去，本次一并暴露。

**修复（`yzplan.spec`）**：
1. 用 `sys.base_prefix` 探测真实解释器 base，并据 `conda-meta` / `Library\bin` 是否存在判断是否 conda → `_IS_CONDA`。
2. **仅在 conda 时**注入额外 `binaries`：
   - `Library\bin` 的 `sqlite3.dll`/`libcrypto-3-x64.dll`/`libssl-3-x64.dll`/`liblzma.dll`/`LIBBZ2.dll`/`ffi.dll`；
   - base `DLLs\` 的标准库 `.pyd`（跳过 `_tkinter_*`/`_test*`/`xxlimited*`）。
3. **标准 CPython venv 时**这些块自动解析为空 → 走 PyInstaller 默认收集，不注入多余文件。
4. `run.bat` 补充 venv 存在性检查、清空 `PYTHONHOME/PYTHONPATH`、固定 `PATH`，避免被其它 Python 干扰。
5. 环境变量从旧的 `YZPLAN_CONDA_BIN` 改为 `YZPLAN_CONDA_ROOT`（备用覆盖，自动探测失效时用）。

**验证**：conda 环境下重建后 `_internal\` 含 `_ctypes.pyd` 及全部 conda DLL，`YZplan.exe` 启动不再崩溃；spec 对标准 CPython venv 逻辑自证为空注入。

### 三、README 整理（功能特性同步 + 构建说明去 anaconda）

- 功能特性改写：主页已从「画布式拖拽」重构为**纵向卡片列表（永不重叠/上移下移/移除/排序持久化）**；RSS 补充分组多选/批量操作、订阅源多标签、条目去图、预览区摘要、通知与自动刷新；新增主窗口屏幕自适应。
- 打包章节重写：明确**推荐使用标准 python.org CPython 建 venv**，`requirements.txt` 全为标准 pip 包、不依赖 anaconda；说明 spec 自动适配两种环境。中英文 `README.md` / `README.en.md` 同步。

### 四、回归

- conda 场景重建 + 启动验证通过（`_ctypes` 等不再报 DLL 加载失败）。