# YZplan 开发日志

> 记录 2026-08-25 进行的打包与环境修复、UI 与托盘退出问题修复、Fluent 风格 UI 转型。

---

## 2026-08-25

### 一、环境与打包

**背景**：原有 `venv` 可复用（Python 3.11.7 + PySide6 6.11.2），但 `pip` 缺失，先补装。

1. **补装 pip / 依赖**
   - `python -m ensurepip --default-pip` 补装 pip（venv 早期未装）。
   - 发现 `psutil`、`feedparser`、`charset_normalizer` 等依赖实际未安装，执行 `pip install -r requirements.txt` 补齐。

2. **PyInstaller 打包（onedir）**
   - 原 `build/yzplan.spec` 指向失效路径（`Y:\...`、conda DLL），已废弃；根目录新建 `yzplan.spec`。
   - 打包反复踩坑，逐一定位：
     - **Qt 平台插件缺失**：`hook-PySide6.py` 因 QtLibraryInfo 隔离子进程无法导入 QtCore 而跳过主体，plugins 未自动收集。解决：spec 中手动把 `PySide6\plugins` 加入 `datas`，并加入 `qt.conf` + 设置 `QT_PLUGIN_PATH`（运行时 hook）。
     - **Python DLL 加载失败 / conda 运行库缺失**：当前 venv 基于 anaconda3 python311，打包依赖其基础解释器。解决：显式打包 `sqlite3.dll`、`libcrypto-3-x64.dll`、`libssl-3-x64.dll`、`liblzma.dll`、`LIBBZ2.dll`、`ffi.dll`。
     - **动态模块未被分析**：`modules/` 子模块由 `registry.py` 运行时动态导入，PyInstaller 静态分析不到。解决：`modules.path_forward`、`modules.sys_info`、`modules.rss_aggregator` 加入 `hiddenimports`，并补 `ctypes.wintypes`。
     - **`platform` / `psutil` 等导入缺失**：依赖被动态导入链切断，逐个补 `hiddenimports`。
   - **产物**：`dist\YZplan\YZplan.exe`（onedir，含 `_internal/` 与 Qt 插件），已在无 anaconda PATH 的干净环境验证启动正常。
   - **再次打包易错提醒**：`qt.conf` 是 spec 引用的数据源，删掉会报 `Unable to find qt.conf`，需保留。

### 二、代码修复

1. **`core/constants.py` — Frozen 路径**
   - 新增 `sys.frozen` 分支：打包后 `PROJECT_DIR`/`DATA_DIR` 改为 exe 同级目录，`data/`（settings.json、app.db）落到 `dist\YZplan\data\`，开发状态下保持原逻辑。

2. **`modules/registry.py` — 模块扫描**
   - 原 `os.listdir(MODULES_DIR)` 在打包后目录不存在（PYZ 存档），改为 `pkgutil.iter_modules()`，开发/打包均可用；`_SKIP` 改为无后缀模块名。

3. **`ui/home_tab.py` — 主页重构（消除重叠、可排序）**
   - 原 QGraphicsView 自由画布默认位置偏移过小（30~40px vs 340×230 卡片），首启即大量重叠。
   - 重写为**纵向卡片列表**：卡片永不重叠；每张卡片标题栏提供 `▲ 上移` / `▼ 下移` / `✕ 移除`；顶部保留 `添加`、`恢复默认顺序`、`清空`。
   - 顺序持久化到 `data/settings.json` 的 `home.order`；兼容旧版 `home.layout`（按 Y 坐标转初始顺序）。

4. **`ui/mainwindow.py` — 托盘退出无法关闭程序**
   - 根因一：`main.py` 设置了 `setQuitOnLastWindowClosed(False)`，关闭窗口不会结束事件循环，程序常驻托盘。
     - 修复：`quit()` 中调用 `self.context.app.quit()` 真正退出。
   - 根因二：首次修复误用 `QtWidgets.QApplication.quit()`，但 `QtWidgets` 是 `__init__` 局部变量，方法内未定义 → `NameError`。
     - 改用 context 中的 QApplication 实例，重新打包后验证托盘退出正常。

### 三、Fluent 风格 UI 转型（PyQt-Fluent-Widgets / PySide6 分支）

- **安装**：`PySide6-Fluent-Widgets 1.11.3`（import 名 `qfluentwidgets`），依赖 `PySideSix-Frameless-Window`、`darkdetect`，已加入 `requirements.txt`。
- **ICU 预载前移**：qfluentwidgets 顶层直接 `from PySide6... import`，必须在任何 Qt 导入前预载 ICU（WinError 127）。`core/qt_bootstrap.py` 增加模块级 `_preload_icu()`（仅 win32）。
- **UI 改造**：
  - `ui/mainwindow.py`：`QTabWidget` → `FluentWindow` + 左侧导航栏（主页/模块/程序设置/关于），沿用 close_to_tray 与 quit 逻辑。
  - `ui/home_tab.py`：卡片改用 `CardWidget`，按钮 `PrimaryPushButton/PushButton/TransparentToolButton`，排序/移除逻辑不变。
  - `ui/modules_tab.py`：模块行改用 `CardWidget` + `SwitchButton`（开/关）+ `PushButton`。
  - `ui/settings_tab.py`：设置项迁移为卡片内 `SwitchButton` 行（开机自启/关闭驻留/启动隐藏）。
  - `ui/about_tab.py`：`SubtitleLabel/BodyLabel/PrimaryPushButton` 排版。
- **打包注意**：qfluentwidgets 资源内置在 `_rc/resource.py`，PyInstaller 直接编入 PYZ；其依赖的 `QtSvg/QtXml/QtSvgWidgets` 已被自动收集，无需额外 hiddenimports。构建完成后干净 PATH 环境验证 exe 启动正常。

### 四、Fluent 主题修复（字体警告 + 黑白不统一）

1. **QFont::setPointSize: Point size <= 0 (-1) 警告**
   - 根因：qfluentwidgets 在 `import` 时注册图标字体（`QFontDatabase.addApplicationFontFromData`），而 main.py 原先**先导入 ui 模块再创建 QApplication**，字体库未就绪导致 pointSize=-1。
   - 修复：把 qfluentwidgets 涉及的 import（`ui.mainwindow`、各 tab、`core.theme`）延迟到 `QApplication` 创建之后；同时 `app.setFont(QFont("Microsoft YaHei", 9))` + 设置 HighDpi 舍入策略。

2. **浅色/深色主题不统一**
   - 根因：qfluentwidgets 主题跟随系统（darkdetect），导航/卡片为深色，但内容区普通 QWidget 用 Qt 默认浅色调色板 → 黑白混杂。
   - 修复：新增 `core/theme.py` 统一调控——`setTheme` 控制 qfluentwidgets；深色时用 `QPalette` 给普通控件设深色底/浅色字，浅色时重置为 standardPalette；切换后全量 `allWidgets().update()`。
   - 设置项：`ui/settings_tab.py` 增加「界面主题」下拉（跟随系统 / 浅色 / 深色），即时保存 `ui.theme` 并生效；`constants.py` 默认 `ui.theme=auto`；启动时 main.py 读取并应用。
   - `smoke_test.py` 同步调整（先建 QApplication 再导入 ui；`.tabs.count()` → `stackedWidget.count()`）。

### 五、Ctrl+G 失灵修复与主页/窗口缩放

1. **Ctrl+G 快捷键彻底修复（此前一直无效）**
   - 根因一：`HotKeyFilter` 未继承 `QAbstractNativeEventFilter`，`app.installNativeEventFilter(self)` 抛 `TypeError`，而 `path_forward.start()` 里 `except Exception` 把它静默吞掉 → 快捷键从未注册。
   - 根因二：PySide6 虚函数签名是 `nativeEventFilter(self, event_type, message, result=None)`（`result` 为可选），之前下划线名+三参写法均不匹配。
   - 修复：`core/hotkey.py` 改为 `class HotKeyFilter(QtCore.QAbstractNativeEventFilter)`，`super().__init__()`，方法名 `nativeEventFilter`、`result` 缺省；并提高 hotkey_id 防冲突。已用模拟 Ctrl+G 验证回调触发；路径捕获与 URL→本地路径解码均正常。
2. **组件颜色与主窗口不匹配**
   - 主页卡片由 QFrame（默认调色板）改回 `CardWidget`（Fluent 主题卡片底色，与模块页/设置页同款），标题 `StrongBodyLabel`、关闭钮 `TransparentToolButton`；画布背景 `palette(window)` 随主题。
3. **右下角同时调整宽高**
   - 手柄加大到 20px（更易抓取）；模拟拖拽验证：右下角手柄拖 (180,130) → 宽高同时 +180/+130；右边柄只改宽、下边柄只改高。
   - 主程序窗口：qframelesswindow 默认 `_isResizeEnabled=True`，WM_NCHITTEST 已含 `HTBOTTOMRIGHT`（右下角双向缩放）。

### 六、今天的产物与验证

- 打包配置：`yzplan.spec`（根目录）
- 运行：`dist\YZplan\YZplan.exe`
- 数据目录：`dist\YZplan\data\`（运行时自动创建 settings.json、app.db）
- 测试：`pytest tests` 全部通过（8 passed）——进程尾部 0xc0000139 为 anaconda+Qt 的既有问题，不影响打包产物。
- 说明：`build\yzplan\yzplan\YZplan.exe` 是 PyInstaller 中间产物，**不可直接运行**，应运行 `dist\YZplan\YZplan.exe`。

---

## 2026-08-29

### 一、主页组件：外框拉伸不同步 + 拖拽换序失效

1. **横向拉伸时卡片外框不同步（内容被裁剪）**
   - 复现确认：Qt 6.11.2 的 `QGraphicsProxyWidget` 中，创建时调用 `setFixedWidth` 会“毒化”宽度传播——之后用 `setFixedSize` 改宽度时，内层 QWidget 变了、proxy 外框不变（垂直拉伸后裁剪区域才显现）。`setMinimumWidth`/`setMinimumSize`/`setFixedHeight` 均不触发此问题，仅 `setFixedWidth` 触发。
   - 修复（`ui/home_tab.py`）：
     - 创建卡片 `card.setFixedWidth(card_w)` → `card.resize(card_w, saved.get("height", _DEF_H))`
     - 手柄 r/c 拖拽改宽 `self._card.setFixedWidth(new_w)` → `self._card.setFixedSize(new_w, self._card.height())`
   - 验证：流式拉伸与手柄拖拽后 proxy 外框宽度均与内层内容一致。

2. **拖动换序完全失效（用户反馈“不太支持拖动换位置”）**
   - 根因：`_Proxy.mouseMoveEvent` 用 `event.button() == QtCore.Qt.NoButton` 判断是否按住左键，但 Qt 中**鼠标移动事件的 `button()` 恒为 `NoButton`**（按住的键在 `buttons()` 里）→ 拖动永远不越过 `_DRAG_THRESHOLD`。
   - 单元测试此前手造事件把 `button` 设为 `LeftButton`（现实中不存在），误成“看似通过”。
   - 修复：改用 `event.buttons() == QtCore.Qt.NoButton`（`ui/home_tab.py`）；测试改为真实语义（`setButton(NoButton)` + `setButtons(LeftButton)`），新增回归用例 `test_proxy_move_event_real_button_state`。
   - 说明：卡片右上角隐藏的 ✕ 是「移除」按钮，非「位置切换」；换序即通过拖动完成。

### 二、RSS 模块两处运行异常

1. **编辑订阅源崩溃**：`_EditFeedDialog(self.owner, feed, self)` 参数顺序错误（`__init__(self, feed, store, parent)` 期望 feed 在前），Module 对象被当作 feed → `feed.get("name")` AttributeError。改为 `_EditFeedDialog(feed, self.owner.store, self)`（对齐 1618 行页面入口写法）。
2. **自动刷新访问已销毁控件崩溃**：`Module._widgets` 只在 `create_home_widget` 追加、从不清理；主页控件销毁后自动刷新仍回调其 `on_refreshed` → `RuntimeError: Internal C++ object (QLineEdit) already deleted`。
   - 修复：新增 `_forget_widget(w)`（按 `x is w` 身份删除）；`create_home_widget` 连接 `w.destroyed` 自动清理；`_on_refreshed` 遍历副本并对 `RuntimeError` 兜底。

### 三、主窗口几何：首启自适应尺寸 + 恢复不生效

1. **首启默认尺寸改为按屏幕自适应（16:10）**
   - 首次启动（`ui_state` 无记录）不再用固定 960×640：宽度取屏幕可用区 80%，高度按 16:10 换算；若高度超出可用区 82%，改为以高度反推宽度；整体不超过可用区并**居中**。`core/constants.py` 默认同步为 1280×800。

2. **`ui_state.apply()` 位置恢复从未生效**
   - 根因：`QGuiApplication.screenAt(QRect(...))` 在 PySide6 6.11 **只接受 `QPoint`** → 每次恢复都抛 TypeError，且被 `except` 静默吞掉、照样返回 True。窗口尺寸能还原（resize 在异常之前），但 `move(x,y)` 永远执行不到。
   - 修复：`screenAt(QPoint(x + w//2, y + h//2))`；`window_geometry()` 挂上 `ui` logger；恢复失败记日志并返回 False（不再假装成功）。

3. **“默认小窗口”真凶：qframelesswindow 原生层缩窗**
   - 实测：`start_hidden=True`（配置中已开启）时窗口创建后**未显示**，事件循环跑一轮后 FluentWindow 被原生层缩回初始 500×500（跨 C++ 侧，Python hook 不到），从托盘打开时即为此前常见的“小窗口”。
   - 修复（`ui/mainwindow.py`）：几何恢复抽成 `_apply_geometry()`，构造时应用一次；**首次 `showEvent` 同步再应用一次**，并补 0ms/60ms 两个延迟兜底重放，确保保存的尺寸+位置最终生效。
   - 验证（真实桌面非 offscreen）：构造后 1202×791@(150,50) → 隐藏缩到 500×500 → 首次显示立即恢复并稳定在 1202×791@(150,50)。

### 四、测试与杂项

- 全量 `pytest` 51 个用例通过（含主页拖动回归、RSS 生命周期验证）。
- `.gitignore` 补充 SQLite WAL/SHM/Journal（`data/*.db-shm`、`*.db-wal`、`*.db-journal`）与单实例锁 `data/yzplan.lock`。
- 修复版本提交：`8e3c129`。

---

## 2026-08-30

### 一、RSS 聚合模块：分组头风格对齐普通订阅源（`modules/rss_aggregator.py`）

1. **`_HeadRow.setStyleSheet` 选择器物配修复**
   - 原逻辑直接把 `QPushButton#...` 的 CSS 规则赋给 `QLabel`（`QPushButton` 选择器不匹配 QLabel，导致样式**从未生效**），且多条规则互相覆盖。
   - 修复：`_HeadRow`（与 `_WrapRow`/`_AutoRow` 一致）先把 `QPushButton`→`QLabel`，再按 objectName（`rssHeadTitle`/`rssHeadCount`）分派，并把基础规则与 `:hover` 规则合并到同一串，一并赋给整个 head。
   - 离屏渲染确认：head 标题样式现为 `QLabel#rssHeadTitle{color:palette(text)...} + :hover{color:#1976d2}`，来源徽标为 `#e8f0fe/#1967d2` 浅蓝胶囊、`border-radius:3px`、`font-size:12px`。

2. **标题去粗体、字号对齐普通条目**
   - 原 `_HeadRow` 标题 `f.setBold(True)` + QSS `font-weight:bold`，比普通订阅条目（`_WrapRow` 11pt 非粗体）更粗更"大"。
   - 移除 `f.setBold(True)`（`_HeadRow.__init__`）与 QSS 中的 `font-weight:bold`，保留 `_TITLE_FONT_PX`（11pt）——与普通条目 `_WrapRow` 完全一致（head `bold=False, size=11.0` vs wraprow `bold=False, size=11.0`）。
   - 聚合成员行本就与普通行共用 `_make_item_row`（非粗体 11pt），无需改动。

3. **保留三交互**（连接逻辑未动）
   - 单击标题 = 预览该分组（`titleClicked`→`_agg_head_preview`）
   - 双击标题 = 默认打开一个来源（`headDoubleClicked`→`_agg_head_open`）
   - 单击来源徽标 = 展开/折叠全部来源（`badgeClicked`→`_toggle_torrent_group`）
   - 徽标文案为 `"N 来源"`（去掉旧 "N 条"）。

### 二、RSS 设置页米黄面板 → 壁纸透出一致性（`_SettingsDialog`）

- **根因**：`QScrollArea` 内容 `QWidget` 的 `autoFillBackground=True` + `#efefef` 整块盖住对话框 `paintEvent` 绘制的壁纸；且只调 `setAutoFillBackground(False)` 无效（被 QSS 重新置 True）。
- **修复**：scroll `viewport()` 与 `content` 均 `setAutoFillBackground(False)`，`content` 设 `objectName("rssSettingsContent")` 并 `QWidget#rssSettingsContent{background:transparent}`——用 objectName 限定透明，避免宽泛 `QWidget{background:transparent}` 破坏 `QLineEdit`/`QSpinBox` 输入框底。
- **验证**：离屏渲染设置对话框中心像素由米黄 `239,239,239` 变为壁纸色 `(350,180)=198,218,225` / `(350,350)=143,144,203`。
- **范围**：按用户确认，仅改设置页 `_SettingsDialog`；分类/关键词/条目预览等小窗口不贴壁纸，保持原样。

### 三、RSS 模块页面壁纸透出确认

- RSS 页嵌入"会画壁纸的窗口"时，列表区/侧边栏/工具栏/预览区容器本就透明，均可透出壁纸色，无需额外改动容器。

### 四、聚合条目多选支持（`modules/rss_aggregator.py`）

1. **`_HeadRow` 新增复选框**
   - `_HeadRow.__init__` 增加 `QCheckBox` + `checkboxToggled` 信号 + `set_check_state`/`check_state` 方法，布局顺序：checkbox → title_label → count_label。
   - 新增 `_on_head_checkbox_toggled(head_hash, checked)`：勾选 head → 勾选/取消该组所有成员条目的 checkbox；取消 → 从 `_selected_hashes` 移除。
   - 新增 `_sync_head_checkbox_state(head_hash)`：遍历成员 checkbox 状态，按全选/部分选/未选设置 head 的 `Checked`/`PartiallyChecked`/`Unchecked`。

2. **`_load_torrent_aggregation` 连接**
   - head 连接 `checkboxToggled` → `_on_head_checkbox_toggled`
   - 每个成员 checkbox 连接 `toggled` → `_sync_head_checkbox_state(head_hash)`
   - 建立 `_head_by_member` 映射（member_hash → head_hash）；加载后初始化每个 head 的 checkbox 状态。

3. **全选联动**
   - `_select_all` 末尾遍历 `_group_children` 调用 `_sync_head_checkbox_state`，确保"全选"/"取消全选"后 head checkbox 状态同步。

### 五、聚合条目换行截断修复

1. **`_HeadRow.heightForWidth` 漏算 checkbox 宽度**
   - 加入 checkbox 后，可用宽度需额外减去 `checkbox.sizeHint().width() + spacing`。原来只减 `count_label + spacing`，导致标题按较宽计算高度 → 实际渲染被 checkbox 挤窄 → 换行更多 → 高度不够 → 截断。
   - 修复：`spawn` 增加 `checkbox.sizeHint().width() + self.layout().spacing()`。

2. **`_toggle_torrent_group` 展开后未重算行高**
   - 展开/折叠后成员行从隐藏变可见，但 `sizeHint` 沿用旧值。
   - 修复：`_toggle_torrent_group` 末尾加 `QTimer.singleShot(0, self._sync_row_heights)` 重新计算。

### 六、聚合条目标题截断与风格对齐

1. **标题数据级截断**
   - 根因：`head_title = (g.get("title") or "(无标题)")[:60]` 在数据层截断到60字符，`[:120]` 无效（已被截断）。
   - 修复：去掉 `[:60]` 和 `[:120]`，标题完整传入 `_HeadRow`，由 word-wrap 自动换行。

2. **行距/字距对齐**
   - `_HeadRow` 标题 QSS padding：`6px 4px` → `2px`（与普通条目 `_WrapRow` 一致）。
   - `_HeadRow` 布局 spacing：`8` → `6`（与 `_AutoRow` 一致）。
   - 离屏验证：head SS 已变为 `padding:2px`，spacing=6，与普通条目完全一致。

### 七、回归与测试

- 更新 `tests/test_rss_sidebar.py::test_agg_head_count_at_end_no_newline` 以匹配新徽标文案 `"N 来源"`。
- 全量 `pytest tests/` 115 个用例通过 + `smoke_test.py` SMOKE OK。

---

## 2026-08-30（晚间 · RSS 四项收尾）

> 本会话针对 RSS 模块完成：选择功能根因修复、条目列表去图片、订阅源多标签（改数据库）、预览摘要。改动文件：`modules/rss_aggregator.py`、`modules/rss_store.py`、`core/constants.py`。

### 一、选择相关功能失效——根因修复（`rss_aggregator.py`）

**现象**：全选（全选/标记已读/未读/删除选中）点了没反应；单条目勾选却正常。

**根因**：PySide6 中 `QCheckBox.stateChanged` 发出的是**普通 int**（未勾选 `0`、勾选 `2`），而代码里写成 `checked = state == QtCore.Qt.Checked`。经实测 `2 == QtCore.Qt.Checked` 恒为 `False`（int 与 `CheckState` 枚举在 PySide6 中不相等）→ 全选永远走「取消选择」分支，把 `_selected_hashes` 清空。此前诊断脚本直接调 `_select_all(QtCore.Qt.Checked)` 传的是枚举，误测为「逻辑正确」。

**修复**：比较改为 `state == QtCore.Qt.CheckState.Checked.value`（`rss_aggregator.py` 全选），并排查出**同源隐患**两处同步修正：
- `chk_notify.stateChanged` 两处 lambda（1413/2831）同型比较。
- 握手验证：单勾选→全选→取消全选→批量已读，`_selected_hashes` / 复选框 / 未读数均正确。

### 二、条目列表不再展示图片（`rss_aggregator.py`）

- 删除 `_make_item_row` 中的缩略图渲染块（`show_thumbnail and it.get("image_url")`）。
- 删除「显示缩略图」切换按钮 `chk_thumbnail` 与 `_toggle_thumbnails` / `_update_thumbnail_btn_text` / `_show_thumbnails` 状态。
- 删除随之失联的异步缩略图工具：`_load_thumb_async`、`_get_network_mgr`、`_network_mgr`、`_widget_alive`。
- `_make_item_row` 精简签名（去掉 `show_thumbnail` 形参），两处调用与 `core/constants.py` 的 `rss.show_thumbnails` 默认键一并移除。

### 三、订阅源支持多标签（改数据库，`rss_store.py` + 对话框）

按用户确认采用「一源多标签」方案，正文模型 `item_sources(hash,tag)` 本就支持一物多标签，限制在**源端**（`feeds.tag` 单列）。

1. **数据层（`rss_store.py`）**
   - 新增 `feed_tags(feed_id, tag)` 表（`_init_schema` 内 `CREATE TABLE IF NOT EXISTS`，兼容既有库）。
   - `add_feed` 接受 `tags` 列表，同事务写入 `feed_tags` 并回填 `feeds.tag`（取首标签作旧版兼容）。
   - `get_feed_tags` / `set_feed_tags` 存取多标签；`get_feed_by_id`、`list_feeds`、`search_feeds`、`list_sidebar` 均附带 `tags` 列表。
   - `update_feed` 拦截 `tags` 键：修复“仅改标签时 `updates` 为空提前 return”导致标签不落库的 bug。
   - `ingest` 入参兼容字符串/列表，把订阅源**全部标签**写入 `item_sources`（每条一标签多行）；`remove_feed` 顺带清理 `feed_tags`。

2. **抓取（`rss_aggregator.py`）**
   - 新增 `_final_tags(f)`（缓存 `f["tags"]`，否则查 `get_feed_tags`，再回退 `[f["tag"] or f["name"]]`）；`_process_feed` 入库改为传标签列表；`_final_tag` 取首标签。

3. **对话框**
   - 添加/编辑订阅源输入框 placeholder 改为「多个标签用逗号分隔」，保存时按 `，`/`,` 拆分；编辑框回填时用 `feed["tags"]` 以逗号拼接显示。
   - 源管理列表标签展示升级为多标签 `(标签: A, B)`。

4. **联动**：`_load_tag_filter` 读取 `item_sources.tag`，多标签入库后自然出现在标签筛选下拉；侧边栏按 `feed_id`（`item_feeds`）过滤，与标签无关，无需改。

### 四、预览区上方新增摘要（`rss_aggregator.py`）

- 预览列外包 `_preview_container`，顶部放置 `_summary_title`（标题，粗体 15px）、`_summary_meta`（标签/更新时间）、`_summary_desc`（去标签纯文本描述，限制 120 高 / 400 字截断），下方才是网页/文本预览。
- 新增 `_set_summary(item_data)`，在 `_display_preview` 开头调用填充。
- 样式不再依赖 `pc` 的 `fg` 键（原 `pc` 仅 panel/panel_soft/border，直接 `.format(**pc)` 曾抛 `KeyError: 'fg'`，首轮全量测试失败后改为不加颜色的透明背景文本）。

### 五、异步竞态崩溃修复（附）

- 修复 `libshiboken: Internal C++ object (QListWidgetItem) already deleted`：`_sync_head_checkbox_state` 遍历 `_group_children` 时，列表重建后条目已销毁。加 try/except `RuntimeError` 跳过失效项，并以有效项数重算 `total`，避免全选/联动时崩溃。

### 六、回归与验证

- 多标签端到端（独立 temp DB）：`add_feed(tags=[...])` → `get_feed_by_id(tags)` / `list_feeds` / `list_sidebar` 均含多标签；`ingest` 后 `get_item.tags` 为 `A | B | C`；`update_feed` 仅改标签也能落库。
- 全量 `pytest tests/`（115 用例，含既有 RSS 侧边栏/聚合/选择）全部通过，`ast` 语法检查 OK。

---

## 2026-08-31（构建环境去 anaconda 依赖 + README 整理）

### 一、build.bat：构建前后备份/恢复运行时数据

- 目的：构建（PyInstaller onedir）会重建 `dist\YZplan\`，若 `data/` 位于被覆盖路径下，运行生成的数据可能被清掉。
- 新增逻辑（`build.bat`）：
  - 构建前把 `data/app.db`、`data/settings.json`、`data/logs/` 移到临时目录 `_data_bak_%RANDOM%`；
  - 构建完成后移回（logs 用 `xcopy` 合并，避免目标目录已存在），最后清理临时目录；
  - 临时目录名带随机数防冲突；即使构建中断，数据也只是暂存不丢失。

### 二、yzplan.spec：解耦 anaconda / 自动适配标准 CPython venv

**背景与根因**：
- 冻结产物报 `ImportError: DLL load failed while importing _ctypes`。
- 根因：当前 `.venv` 基于 anaconda3 python311，编译扩展模块（`_ctypes.pyd`、`_socket.pyd`、`_ssl.pyd`、`_sqlite3.pyd` 等）与 `sqlite3.dll`、`libssl/libcrypto`、`ffi.dll` 等**都位于 base conda 根目录（venv 之外）**，PyInstaller 不会自动收集 → exe 启动即崩。
- 旧 spec 的 conda DLL 路径默认指向 `~/anaconda3`（`C:\Users\yuan` 下不存在）→ 这些 DLL 其实没被打进去，本次一并暴露。

**修复（`yzplan.spec`）**：
1. 用 `sys.base_prefix` 探测真实解释器 base，并据 `conda-meta` / `Library\bin` 是否存在判断是否 conda → `_IS_CONDA`。
2. **仅在 conda 时**注入额外 `binaries`：
   - `Library\bin` 的 `sqlite3.dll`/`libcrypto-3-x64.dll`/`libssl-3-x64.dll`/`liblzma.dll`/`LIBBZ2.dll`/`ffi.dll`；
   - base `DLLs\` 的标准库 `.pyd`（跳过 `_tkinter_*`/`_test*`/`xxlimited*`）。
3. **标准 CPython venv 时**这些块自动解析为空 → 走 PyInstaller 默认收集，不注入多余文件。
4. `run.bat` 补充 venv 存在性检查、清空 `PYTHONHOME/PYTHONPATH`、固定 `PATH`，避免被其它 Python 干扰。
5. 环境变量从旧的 `YZPLAN_CONDA_BIN` 改为 `YZPLAN_CONDA_ROOT`（备用覆盖，自动探测失效时用）。

**验证**：conda 环境下重建后 `_internal\` 含 `_ctypes.pyd` 及全部 conda DLL，`YZplan.exe` 启动不再崩溃；spec 对标准 CPython venv 逻辑自证为空注入。

### 三、README 整理（功能特性同步 + 构建说明去 anaconda）

- 功能特性改写：主页已从「画布式拖拽」重构为**纵向卡片列表（永不重叠/上移下移/移除/排序持久化）**；RSS 补充分组多选/批量操作、订阅源多标签、条目去图、预览区摘要、通知与自动刷新；新增主窗口屏幕自适应。
- 打包章节重写：明确**推荐使用标准 python.org CPython 建 venv**，`requirements.txt` 全为标准 pip 包、不依赖 anaconda；说明 spec 自动适配两种环境。中英文 `README.md` / `README.en.md` 同步。

### 四、回归

- conda 场景重建 + 启动验证通过（`_ctypes` 等不再报 DLL 加载失败）。

---

## 2026-09-02

### 一、新增功能总览

1. **便签/待办事项模块** (`modules/todo_notes.py`)
   - SQLite 持久化（`data/app.db` 的 `todo_notes` 表）
   - 主页卡片：勾选完成、快速添加、优先级颜色标签
   - 独立页面：搜索/筛选/排序、新增/编辑/删除、截止日期高亮

2. **标题栏自定义按钮** (`ui/mainwindow.py`)
   - 自定义 `FluentTitleBar` 子类，在 `buttonLayout` 中插入自定义按钮
   - 设置按钮（⚙）：弹出设置对话框
   - 日志按钮（📋）：弹出日志查看窗口 + 异常红点/数字 badge 提示
   - 重启按钮（🔄）：释放单实例锁后重启程序

3. **msedgewebview2.exe 网络管控模块** (`modules/webview_control.py`)
   - 核心：通过 `netsh advfirewall` 添加/删除 Windows 防火墙出站+入站拦截规则
   - 辅助：`psutil` 实时监控 msedgewebview2.exe 进程和网络连接
   - UAC 运行时提权（`ShellExecuteW` + `runas`）
   - 不杀进程，仅通过防火墙规则管控网络访问

4. **快捷方式 .lnk 隐藏 CMD 窗口** (`create_shortcut.py`)
   - 使用 `win32com.client` 创建指向 `pythonw.exe` 的快捷方式
   - `pythonw.exe` 为 GUI 子系统解释器，不创建控制台窗口
   - 保留 `run.bat`（使用 `python.exe`）作为调试备用

5. **标题栏日志按钮 + 异常提示** (`core/logger.py` + `ui/mainwindow.py`)
   - 全局错误计数器：`ERROR`/`CRITICAL` 日志自动递增
   - 红点 badge：绘制在日志按钮右上角，显示未读错误数
   - 日志弹窗：复用 `SettingsTab` 日志 UI，打开时重置计数

6. **标题栏重启按钮** (`core/restart.py`)
   - exe 模式：直接运行 `sys.executable`
   - 开发模式：优先 `pythonw.exe`，否则 `python.exe`
   - 重启前释放 `QLockFile` 单实例锁

### 二、修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `modules/todo_notes.py` | 新建 | 便签/待办事项模块 |
| `modules/webview_control.py` | 新建 | WebView2 网络管控模块 |
| `core/restart.py` | 新建 | 重启逻辑 |
| `create_shortcut.py` | 新建 | .lnk 快捷方式创建脚本 |
| `core/logger.py` | 修改 | 添加错误计数器 + 回调 |
| `ui/mainwindow.py` | 修改 | 自定义标题栏 + 三个按钮 |
| `ui/settings_tab.py` | 修改 | 提取日志 UI 为独立类 |
| `main.py` | 修改 | 传递 `si` 引用到 context |

### 三、技术决策

- **网络管控**：采用 Windows 防火墙 API（`netsh advfirewall`），非杀进程方案。杀毒软件/企业管控均基于 WFP 内核引擎，普通应用通过 `netsh` 即可实现程序级网络拦截。
- **隐藏 CMD**：采用 `.lnk` 快捷方式指向 `pythonw.exe`，`.bat` 文件本身由 `cmd.exe` 解释，无法避免控制台窗口。
- **标题栏按钮**：继承 `FluentTitleBar`，在 `buttonLayout` 中 `insertWidget(0, ...)` 插入 `FluentTitleBarButton`，保持 Fluent 风格一致。
- **管理员权限**：防火墙规则操作通过 `ShellExecuteW("runas")` 触发 UAC，不强制启动时提权。

### 四、当日修复（2026-09-02 第二轮）

1. **设置按钮导致导航崩溃**（`ui/mainwindow.py`）
   - 根因：设置按钮直接复用主窗口导航页的 `settings_tab.widget`，放入 `QDialog` 时被从 `stackedWidget` 中移除，导航切换时 `setCurrentWidget(-1)` 报 `The index '-1' is illegal`。
   - 修复：改为每次点击新建独立 `SettingsTab(context)` 实例，共享同一 `context.config`，不触碰主窗口组件。

2. **ComboBox int icon 崩溃**（`AttributeError: 'int' object has no attribute 'icon'`）
   - 根因：qfluentwidgets `ComboBox.addItem` 签名是 `addItem(text, icon=None, userData=None)`。把数据放第二个参数（icon 位）时：字符串不崩但 `currentData()` 返回 None（筛选失效）；**int 直接崩溃**（`.icon()` 对 int 调用）。
   - 修复：所有 qfluentwidgets ComboBox 的数据改用 `addItem(text, userData=...)`。涉及 `modules/todo_notes.py`（优先级 int、筛选/排序）、`ui/settings_tab.py`（主题、日志级别/来源）、`ui/log_viewer.py`（日志级别/来源）。原生 `QtWidgets.QComboBox` 不受影响（第二参数本就是 userData）。

3. **日志展开/收缩**（需求1，`ui/log_viewer.py` + `ui/settings_tab.py`）
   - 消息列默认单行截断（超 200 字符显示省略号），悬停显示完整 tooltip。
   - 工具栏新增 `展开消息` / `收缩消息` 按钮，一键切换所有行完整/单行。
   - 双击消息单元格可单独展开/收缩该行；展开时按内容自动增高行以换行显示完整文本。

4. **毛玻璃模糊范围调整**（需求2，`ui/settings_tab.py` + `core/constants.py`）
   - 滑块范围由 `5~80` 改为 `0~40`（40px 已相当模糊，5px 作为下限偏高，允许 0 即不模糊）。
   - 默认值由 35 改为 20。
### 五、新增功能（2026-09-02 第三轮）

1. **便签/日志悬浮高亮看不清字**（需求1）
   - 便签主页列表与日志表格改为「弱化悬浮/选中高亮」：hover 用极淡中性背景、选中用淡灰背景，不再用亮蓝/亮白背景盖住彩色文字。
   - 涉及 `modules/todo_notes.py`、`ui/log_viewer.py`、`ui/settings_tab.py`。

2. **便签面板增强**（需求2）
   - 直接修改：双击标题/内容/类别单元格内联编辑，实时写库（`table.itemChanged`）。
   - 优先级扩展到 4 级：低/中/高/紧急；新增「类别」字段与类别筛选下拉。
   - 支持直接复制（含多选批量复制到剪贴板），右键菜单 + 「复制」按钮。
   - 数据库新增 `category` 列（旧库自动 `ALTER TABLE` 迁移）。

3. **MCP 接口**（需求3）
   - 新增 `mcp_server.py`：实现 MCP 协议的 stdio 与 HTTP 双传输，暴露工具：todo CRUD、系统信息、日志查询、RSS 管理、GUI 通知（gui_notify）。
   - GUI 通知方案：MCP 写 `data/mcp_inbox/*.json`，运行中的 GUI 经 `core/tray.py` 新增的 `start_mcp_inbox_watcher` 轮询并弹系统托盘通知。
   - `main.py` 启动时清理残留 inbox 并启动监听。
   - 项目级 `opencode.json` 注册了 opencode 的 MCP 客户端（`mcp_server.py stdio`），重启 opencode 后可调用。

4. **程序性能监测**（需求4）
   - 新增 `core/perf.py`：`with perf.timed(name)` 计时工具 + 内存统计 + CSV 导出 + 全局开关。
   - 新增 `modules/perf_monitor.py`：性能监测模块，提供进程资源面板（CPU/内存/线程/句柄，可调节刷新间隔）、关键操作耗时统计表、导出 CSV / 清空 / 开关采集；含主页精简卡片。
   - 在 `rss_aggregator._process_feed` 的网络抓取处接入 `perf.timed("rss.fetch.<name>")` 计时。

---

## 2026-09-02（第三轮后续：交互增强 + MCP 入口 + 函数级性能监测）

**需求3a：便签交互增强**
- 便签页面 7 列表格中原双击（编辑 / 弹详情）改为：**单击**标题/内容/类别列直接**行内编辑**（快速修改），**双击**仍弹出 `_TodoEditDialog` 详情窗口。
- 为避免单击与双击手势冲突，用 220ms 单发 `QTimer` 延迟触发行内编辑；收到双击则取消延时、改为弹详情。
- 标题/内容/类别列改为 `Interactive`（可拖拽调宽），`setStretchLastSection(False)`；其余列自适应内容。

**需求2b：MCP 服务器设置入口**
- `ui/settings_tab.py` 新增「MCP 服务器」卡片：
  - `启用 MCP HTTP 服务` 开关（开关即保存配置并在应用内后台启动/停止 `127.0.0.1:8765` WSGI 服务）。
  - 显示 stdio / HTTP 两条启动命令（只读输入框 + 一键复制）。
  - 实时显示已注册工具数量（当前 15）。
  - `测试连接` 按钮：后台线程调用 `todo_stats` 验证 MCP 工作正常并弹 InfoBar。
- `_load()` / `main.py` 会读取 `mcp.enabled`，开机启动时自动拉起 HTTP 服务。

**需求1b：函数级性能监测**
- `core/perf.py` 扩展为三档函数级监测，均接入既有的 `perf` 统计与开关：
  1. **`@trace()` 装饰器**：包装单个函数，按 `模块.函数名` 记录调用次数与耗时（复用 `_records`，直接显示在耗时统计表）。
  2. **系统级函数采样器**（`profile_start/profile_stop/profile_snapshot`）：用 `sys.setprofile` 统计每个函数被调次数与自用耗时，用于定位 CPU 热点；页面「函数采样器」标签有开关 + 立即快照。
  3. **线程实时栈快照**（`thread_snapshots`）：经 `sys._current_frames()` 抓取各线程当前调用栈，用于定位卡顿/死锁现场；页面「线程栈」标签一键抓取。
- `modules/perf_monitor.py` 独立页新增「函数级监测」分组（QTabWidget 两页），页销毁时自动停止采样器。
- 已在 `todo_notes.get_todos` 与 `rss_store.fetch_feed` 加 `@trace()` 示例埋点。
- 全量 115 项 pytest + `smoke_test.py` 通过；MCP HTTP `GET /tools` 返回 200 验证通过。

---

## 2026-09-02（MCP 端到端验证 + 修复发现的 2 个 bug）

**验证**
- 用 HTTP 端到端调用通过 MCP 操纵了真实程序：`initialize`、`tools/list`（15 个工具）、`todo_stats`、`todo_add`→`todo_update`→`todo_list`→`todo_delete` 全链路、`gui_notify`（写 `mcp_inbox/*.json` 由运行中 GUI 轮询弹托盘通知）、`rss_list/rss_recent`、`logs_get/clear`、`system_info/system_resources` 均正常。
- stdio 传输（opencode 即用）同样验证通过：喂 4 条 JSON-RPC 消息，返回 initialize / 15 工具 / todo_stats / 未知工具错误码。
- 确认 `start_mcp_inbox_watcher` 的 QTimer 在主线程创建（`QApplication` 之后 `app.exec()` 之前），无线程 bug。

**Bug 1：`rss_recent` 缺 `sqlite3` 导入 → 500**
- 症状：`rss_recent` 直接 `sqlite3.connect(...)` 但函数内未 `import sqlite3`，模块级也未导入 → `NameError` → HTTP 500。
- 修复：`mcp_server.py` 的 `rss_recent` 内补 `import sqlite3`。

**Bug 2：工具调用异常直接抛出，崩溃 stdio 服务/返回 500**
- 症状：未知工具、或工具内部校验失败（如 `todo_update` 找不到 id）时 `handle_message` 直接抛 `ValueError`：
  - stdio：`run_stdio` 未捕获 → 整条服务崩溃退出。
  - HTTP：WSGI → 500。
- 修复：`tools/call` 分支用 try/except 把 `KeyError`（未知工具）/`ValueError`（参数/业务错误）→ 错误码 `-32602`，其余异常 → `-32603`；`run_stdio` 外层再兜底，单条坏消息不影响服务存活。

**回归**
- 新增 `tests/test_mcp.py`（11 项）：工具清单、todo CRUD 往返、系统信息、日志、rss_recent 不崩溃、rss CRUD 往返、gui_notify、initialize/tools-list（校验剥离 handler）、tools/call 成功与未知工具/内部异常转错误码。
- 全量 126 项 pytest + `smoke_test.py` 通过；DB 数据保持用户原状（4 待办、2 RSS 源），测试自清理无残留。

---

## 2026-09-02（用 MCP + 性能监测诊断程序卡死）

**现象**
- 日志显示最近一次启动后，`RSS模块已启动`（23:01:20）之后再无任何日志 → 疑似主线程在启动早期卡死。
- 日志中还发现一条已修复的历史异常：`_TodoEditDialog` 里的 `ComboBox.setEditable`（21:50:49），当前代码已改用 `EditableComboBox`，无残留。

**诊断方法（用户要求：用 MCP 调用性能监测模块）**
- 在 `mcp_server.py` 新增 5 个**性能监测诊断工具**（由之前写的 `core.perf` 提供数据）：
  - `perf_threads`：抓取当前进程各线程函数栈快照（诊断卡死/死锁现场的关键工具）。
  - `perf_stats`：函数/操作耗时统计与热点。
  - `perf_profile`：系统级函数采样器热点。
  - `perf_enable` / `perf_reset`：开关与清空采集。
- 新增后 MCP 工具数 15 → **20**，均已通过测试与 HTTP 端到端验证。
- **关键修正**：把 MCP HTTP 服务启动从 `main.py` 后半段提前到 `config` 创建之后、`MainWindow`/UI 构造之前。
  - 这样即使后续 UI 初始化（真实 GUI 下）卡死，MCP `perf_threads` 也已在运行，可远程抓取卡住的线程栈。
  - 验证：offscreen 启动时 MCP `GET /tools` 200，应用运行期间可远程调用诊断工具。
- `tests/test_mcp.py` 增加 `test_perf_tools`（12 项全通过）；smoke_test 通过。

**结论**
- 离线（offscreen）无法复现卡死 —— 应用可达 `app.exec()` 正常运行，说明是真实 GUI 环境特有或间歇性。
- 已备好远程诊断能力。下一步：用户重新运行应用，若卡死，立即通过 MCP `perf_threads` 抓取主线程栈定位阻塞点。

---

## 2026-09-02（MCP 远程诊断命中卡死根因并修复）

**诊断过程（用户当场卡死，用 MCP 抓线程栈）**
- 用户复现卡死后，MCP `perf_threads` 成功抓到了主线程栈：
```
  main → mouseReleaseEvent(modules_tab.py) → _open_page → _refresh_stats(perf_monitor.py)
       → stats(core/perf.py) → <listcomp> → _hook(core/perf.py, with _lock)  ← 卡死在这
```
- 根因：**函数采样器 `_hook` 两个叠加 bug 导致主线程死锁**：
  1. `_hook` 里 `return _hook`：`sys.setprofile` 的 profile 函数返回非 None 时，会让该函数自身的内部调用（`time.perf_counter`/`dict.setdefault` 等）也触发本钩子 → 无限递归。
  2. `_hook` 复用 `stats()` 同一把**不可重入**的 `_lock`：用户在性能页「函数采样器」开启采样后，`_refresh_stats → stats()` 持有 `_lock` 迭代时触发 `_hook`，`_hook` 再抢 `_lock`（非重入）→ 死锁，主线程永久卡死，GIL 被占导致 MCP 线程也失联（先能抓一次栈，随后完全无响应）。

**修复（core/perf.py）**
- `_hook` 改为 `return None`（不再让采样器剖析自身内部调用），彻底消除递归。
- 采样器改用**独立锁** `hook_lock`（threading.Lock），与 `stats()/record()` 共用的 `_lock` 隔离，杜绝迭代期抢锁死锁。
- 已验证：采样器开启 + 主线程 20 万次 `stats()` 迭代不再卡死，采样/停止正常（修复前同条件下会瞬间死锁）。

**回归**
- `tests/test_mcp.py` 新增 `test_profiler_hook_no_freeze`（复现并防止回归），全量 128 项 pytest 通过。
- 注意：卡死的运行进程（pid 4660 pythonw.exe）内存里仍是旧代码，需关闭后重新启动应用才能加载修复。


---

## 2026-09-02（WebView2 管控模块重构：按第三方程序管控）

**需求**：原始页面只按 msedgewebview2.exe 路径做防火墙规则 + 进程列表，无法区分属于哪个宿主程序。
按用户选定方案（杀进程 + 持续监控其 WebView2 子进程）重构。

**实现（modules/webview_control.py）**
- 宿主检测：`scan_hosts()` 遍历 msedgewebview2.exe 进程，向上追父进程链找到发起它的第三方宿主程序（exe 路径作唯一标识），聚合出程序名/程序地址/链接状态/WebView2 进程数/连接数。
- 封禁状态持久化到 config（`webview.blocked_hosts`），重启后仍生效。
- `kill_host_webview()`：按宿主 exe 终止其 WebView2 子进程。
- 后台监控线程（1 秒轮询）：凡宿主在被拦截集合中，持续杀掉新冒出的该宿主 WebView2 子进程（程序重新打开 WebView2 也会被立即终止）。
- 页面重构为**单个表**：列 = 程序名 / 程序地址 / 链接状态 / 封禁开关；补显已封禁但当前未运行的宿主；右键菜单（打开位置/结束其 WebView2 进程/放行）。链接状态区分：已拦截(红)、未运行(灰)、连接中(绿)、运行中·无连接。
- 主页组件改为"全部拦截/全部放行"当前扫描到的第三方程序。
- 修复 psutil connections() 弃用告警 → 
et_connections()。

**测试**
- 新增 `tests/test_webview_hosts.py`（7 项）：blocked 读写往返、大小写不敏感匹配、scan_hosts 结构、kill 无崩溃。
- 全量 `pytest tests/ -q` 134 项通过；`smoke_test.py` SMOKE OK。

---

## 2026-09-02（便签模块自适应窗口）

**需求**：便签模块整体边框/表格大小随窗口拉动自适应。

**实现（modules/todo_notes.py）**
- 新增 `qfluwidgets_col_stretch()` 复用辅助：给 QTableWidget 装事件过滤器，监听 Resize 事件；表格视图宽度变化时，按初始测量比例把剩余宽度重新分配给可拖动列（标题/内容/类别）。
- 便签页调用该辅助：`stretch_cols=(0,1,2)`（可拖拽 + 自适应），固定列（优先级/截止/状态/创建）用 ResizeToContents。
- 保留既有「列可拖拽 + 单击行内编辑」能力；表体本身通过布局 stretch 跟随窗口纵向扩展。
- 过滤器对象由局部 `_stretch` 变量持有，避免被 GC 回收。
- offscreen 实测：页面构建正常，_Filter 事件过滤器已装上，resize 会按比例重分配列宽。

**测试**：全量 `pytest tests/ -q` 134 项通过；`smoke_test.py` SMOKE OK。

---

## 2026-09-02（全程序表格列宽自适应窗口）

**需求**：所有列表/表格统一自适应窗口；日志模块拉动后列宽不还原；最右侧边框贴合窗口；所有列可拖拽；窗口拉伸时按百分比等比缩放。

**根因（日志列宽还原）**：原日志表格列用 ResizeToContents/Stretch，_refresh_logs 每 3 秒重建 item 时被重算宽度。

**实现**
- 新增共享工具 `ui/adaptive_table.py`（`make_adaptive_table(table, min_column_width=40)`）：
  - 所有列设为 Interactive（可拖拽）。
  - 事件过滤器监听表格 Resize，监听表头 sectionResized：用户拖拽 → 记录该列为基准列宽。
  - (表格 Resize) → 按 `viewport宽/基准总宽` 因子对所有列百分比等比缩放；末列吸收取整误差保证右边界贴合窗口。程序化缩放置 `_resizing` 标志防止反向记录基准。
  - 首次显示按表头+前 50 行内容一次性定初值后冻结为基准。
- 应用到 7 处表格：日志查看器/设置页日志/便签/WebView2/性能监测 2 处/系统信息；删除便签旧 `qfluwidgets_col_stretch` 并迁移为共享工具。

**测试**
- 新增 `tests/test_adaptive_table.py`（4 项）：列均 Interactive、首测基准与右缘贴合、用户拖拽更新基准、拉伸按比例缩放且右缘贴合。
- 全量 `pytest tests/ -q` 138 项通过；smoke_test SMOKE OK；实机确认日志列均 Interactive。

---

## 2026-09-03（便签模块改进入户验收 + 列宽还原根因修复）

**需求（来自 todo_notes 便签 id 3/5/22）**：类别/优先级/状态点击下拉编辑、快速编辑多行、行内编辑全选看不清、行高太矮、日期选择器主题、列宽特定宽度后突然还原、模块窗口不抢焦点、性能热点自动排序。

**实现（modules/todo_notes.py）**
- 新建 `_TodoItemDelegate`（QStyledItemDelegate）：类别/优先级/状态三列内联下拉编辑（createEditor → QComboBox），标题列默认行编辑且 `setEditorData` 中 `selectAll` 后 `deselect()`（不全选，解决高亮看不清）。
- 内容列(1)单击改打开 `_TodoEditDialog`（多行编辑），content item 显式清除 `ItemIsEditable`；priority/status item 置 `ItemIsEditable`。
- `on_item_changed` 处理 col 0/2/3/5（title/category/priority=UserRole/done=UserRole）持久化到 DB。
- **修复卡死 bug**：delegate `setModelData` 修改 item 时同步触发 itemChanged → refresh() 重建表格并销毁正在编辑的 item（`already deleted`）。改为 `request_refresh()` 延迟到 singleShot(0) 刷新，编辑完结后再重建。
- 日期选择器新增 `_apply_date_theme()`：按 `resolve_dark("auto")` 给 QCalendarWidget 加主题 QSS（黑色背景下字体可辨）。
- 行高 `setDefaultSectionSize(30)`；多选批量复制并入右键菜单。
- 模块级导入改 `_, QtCore, QtGui, QtWidgets = import_qt()`（delegate 需 QtGui）。

**实现（ui/adaptive_table.py — 列宽“突然还原”根因）**
- 根因：表格自身 Resize 事件触发时 `viewport().width()` 仍是旧值 → reflow 用过期几何；且 `Interactive` 内容宽成为最小宽阻止收缩。
- 修复：`_schedule_reflow()` 延迟到 zero-timer（`_pending` 去重）用落定后的几何计算；reflow 保证 sum==viewport（末列吸收取整），窄窗口按比例压缩不横向溢出；init 加 `setMinimumWidth(0)`/sizePolicy Expanding/`setMinimumSectionSize(20)`。
- offscreen 验证：narrow(360)/wide(860)/back(760) 各时点列总宽恒等于 viewport。

**其他**
- `ui/modules_tab.py`：模块窗口改 modeless（`dlg.show()` + `setWindowModality(NonModal)`），不再抢焦点。
- 性能监测 `profile_snapshot` 已按 `self_s` 降序（自动排序已满足）。

**测试**
- 新增 `tests/test_adaptive_table.py` 2 项回归：resize 后 reflow 恒贴合 viewport；拖拽基准在 reflow 后保留。
- 新增 `tests/test_todo_notes_ui.py`（3 项）：页面构建 + 列均 Interactive + delegate 装在表上；类别/优先级/状态列编辑器为 QComboBox、标题为 QLineEdit；行高 ≥28 且内容列不可编辑。
- 手工脚本验证：优先级/状态下拉编辑正确写回 DB 且不崩。
- 全量 `pytest tests/ -q` **143 项通过**；smoke_test SMOKE OK；所有改动文件 py_compile 通过。

---

## 2026-09-03（RSS模块打开卡死修复 + MCP跨线程安全 + 飞行记录器）

### 一、RSS模块打开卡死修复（核心）

**问题**：打开 RSS 模块 UI 卡死约 14.4 秒。

**根因定位（cProfile）**：
- `_load_torrent_aggregation` 调用 `get_aggregation_torrent_items` **1000+ 次**（200 groups × 每个 group 一次查询），每次打开新 SQLite 连接（共 1008 次 connect），DB 查询耗时 8.2s。
- sidebar `reload` → `setCurrentRow` → `_on_selection_changed` → `_load_items` 级联触发，重复执行上述查询。
- UI 层：`setItemWidget` 1945 次 × 1.5ms = 2.9s，`_make_item_row` 1945 次 × 0.5ms = 0.9s。

**修复（3 处改动）**：

1. **`modules/rss_store.py` — 批量查询方法**
   - 新增 `get_all_aggregation_torrent_items(agg_id)`：一条 SQL 联表查询获取整个聚合全部成员，Python 端按 `torrent_hash` 分组返回 `{hash: [items]}`。
   - 原方法 `get_aggregation_torrent_items`（逐 group 查询）保留向后兼容。
   - 查询耗时：**0.019s**（原 1000+ 次查询 8.2s，提升 430 倍）。

2. **`modules/rss_aggregator.py:3152` — 重写 `_load_torrent_aggregation`**
   - 调用 `store.get_all_aggregation_torrent_items(agg_id)` 一次获取全部数据，替代逐 group 调用。

3. **`modules/rss_aggregator.py:2173` — sidebar `reload` 修复级联**
   - `reload()` 中 `self.list.blockSignals(True)` → 设置选中 → `blockSignals(False)` → 手动调用一次 `page.on_sidebar_selection_changed()`。
   - `_on_sort_changed` 中移除冗余的 `page.on_sidebar_selection_changed()` 调用（reload 已触发）。

**效果**：

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| `create_page` | 5.8s | 1.7s |
| `_load_items` | 2.7s | 0.5s |
| `reload` | 5.9s | 0.6s |
| **总计** | **14.4s** | **2.8s** |
| SQLite 查询次数 | 1000+ | 1 |
| SQLite 连接数 | 1008 | ~10 |

### 二、MCP 测试连接卡死修复

**问题**：设置页点击「测试连接」后 UI 卡死。

**根因**：`_test_mcp` 在后台线程直接调用 `InfoBar.success()` 操作 Qt widget，Qt 非线程安全，主线程事件循环被阻塞。

**修复**（`ui/settings_tab.py`）：
- 新增 `_MCPBridge(QObject)` 信号桥：后台线程计算完成后 `emit(done, msg)`，主线程 `QueuedConnection` 自动排队接收。
- `_on_mcp_result` 只在主线程操作 GUI widget。

### 三、应用启动时 MCP HTTP 服务提前启动

`main.py`：若 `mcp.enabled`，在 UI 初始化前启动 MCP HTTP 线程。这样即使 UI 初始化卡死，也能通过 MCP 接口远程调用 `perf_threads` 等工具诊断卡住的线程。

### 四、飞行记录器（perf watchdog）

- `core/perf.py`：守护线程每 2s 检查主线程响应，超过阈值抓取主线程栈并落盘到 `data/logs/`。
- `main.py`：启动 `start_watchdog()` + 主线程心跳 QTimer（1s 间隔）。
- `modules/perf_monitor.py`：性能监测模块新增「运行状态/卡死排查」tab。

### 五、其他改动

- `core/tray.py`：新增 `start_mcp_inbox_watcher`，轮询 `data/mcp_inbox/` 目录，弹托盘通知（MCP 接口可跨进程控制 GUI）。
- `core/logger.py`：新增 `error_count` 统计与 `on_error_count_changed` 回调机制。
- `core/constants.py`：毛玻璃模糊半径默认值 35→20。
- `modules/rss_store.py`：`fetch_feed`/`scrape_page` 加 `@trace()` 性能埋点。

### 六、测试

- 全量 `pytest tests/ -q` **144 项通过**；所有改动文件 `py_compile` 通过。

---

## 2026-09-03（便签模块复核 + 内容修改自动设为待办）

**需求（来自 todo_notes id 3）**：条目内容被修改时自动将该条目设为「待办」（已完成条目因新增/改动内容而重置为待办）。

**实现（modules/todo_notes.py）**
- 新增 `_maybe_reset_done_on_content_change(todo_id, old_content, new_content)`：仅当内容(content)字段变化时 `update_todo(todo_id, done=0)`。
- 接入两处内容编辑保存路径：内容列单击弹出的 `_TodoEditDialog` 保存、以及双击/编辑按钮的 `on_edit` 保存。
- 其余 id 3 已实现项（多选批量复制、类别/优先级/状态下拉框、日期主题、单击快速修改、快速编辑多行）经代码复核确认生效。

**测试**
- 新增 `tests/test_todo_notes_ui.py` 2 项：内容变化重置为待办；内容未变保持原状态。
- 全量 `pytest tests/ -q` **145 项通过**；smoke_test SMOKE OK；改动文件 py_compile 通过。

---

## 2026-09-03（便签模块：复选框批量复制 + 内容行内多行编辑）

**需求（来自 todo_notes id 3 更新）**：
- 多选批量复制带复选框；
- 鼠标单击"内容"列也应进入快速修改（行内多行编辑），双击再弹出详情框；快速编辑默认不全选；
- 快速编辑模式支持显示多行内容；日期选择主题与主程序一致。

**实现（modules/todo_notes.py）**
- 新增 8 列表格，首列为复选框列（`COL_CHECK`），用 `COL_*` 常量统一列索引；工具栏"复制"优先取勾选行，其次选中行。
- `_TodoItemDelegate`：内容列改用 `QPlainTextEdit`（多行）行内编辑（`setFrameStyle(NoFrame)`）；标题/类别/优先级/状态下拉/单行逻辑保留，编辑进入时不全选（deselect）。
- `on_item_changed` 新增内容列持久化并自动 `done=0`（沿用"内容修改设为待办"功能）。
- 单击任意可编辑列（含内容）延迟触发 `table.editItem` 行内编辑；双击停止定时器并打开 `_TodoEditDialog` 详情。
- 日期主题 `_apply_date_theme` 保留（`_TodoEditDialog` 内应用，随 `resolve_dark("auto")` 适配深/浅色）。

**测试**
- 更新 `tests/test_todo_notes_ui.py`：下拉列/标题/内容编辑器类型断言、复选框列可勾选、内容列可编辑。
- 全量 `pytest tests/ -q` **147 项通过**；smoke_test SMOKE OK；改动文件 py_compile 通过。

---

## 2026-09-03（便签模块：复选框修复 + 全选 + 内容行内多行可用性）

**需求（来自 todo_notes id 3 再次更新）**：
- 复选框之前不可勾选，且没有全选功能；请支持 shift/ctrl 模式选择；
- 日期选择主题与主程序一致；
- 快速编辑内容多行虽然显示但几乎不可用（行高只有单行、滚动失效），建议进入多行时默认展开整行显示全部内容。

**实现（modules/todo_notes.py）**
- **复选框不可勾选根因**：`on_item_changed` 末尾无条件 `request_refresh()`，勾选复选框→itemChanged→全表刷新重建→复选状态被重置。改为仅在标题/内容/类别/优先级/状态等数据列分支内调用 `request_refresh()`；复选框列不触发刷新。
- **全选**：工具栏新增「全选」QCheckBox，`_on_select_all_toggled` 勾选/取消全部行复选框；`_update_select_all_state` 随 `refresh()` 同步全选状态（blockSignals 防回环）。shift/ctrl 多选沿用 `ExtendedSelection`。
- **批量复制**：`on_copy` 优先取勾选行（`_checked_rows`），无勾选时退回选中行。
- **内容行内多行可用性**：单击内容列前 `_expand_row_for_content` 按内容长度与列宽估算行数并展开行高（上限 240px）；编辑器关闭时 `_TodoItemDelegate.destroyEditor` 把该行恢复默认高度。
- **单击/双击定时器重构**：去掉易报错的 `timeout.disconnect()`，改为持久连接 + `_pending_edit` 闭包变量；双击清空 pending 打开详情框。
- **日期主题**：`_apply_date_theme` 增强——除 QSS 外直接设置日历 `QPalette`（WindowText/Text/ButtonText/Window/Base/HighlightedText），兜底保证深浅色文字对比清晰。

**测试**
- 更新/新增 `tests/test_todo_notes_ui.py`（7 项）：复选框列可勾选、内容列可编辑、内容编辑器为多行 QPlainTextEdit、destroyEditor 恢复行高、全选逻辑（功能脚本验证）。
- 全量 `pytest tests/ -q` **150 项通过**；smoke_test SMOKE OK；改动文件 py_compile 通过。

---

## 2026-09-03（便签模块：shift/ctrl 复选 + 内容折行换行 + MCP 重启接口）

**需求（来自 todo_notes id 3 再次更新）**：
- 复选框支持 shift/ctrl 模式选择（并与表格行选择联动）；
- 日期选择主题仍黑底混字，需与主程序完全一致；
- 删除内容后之前文字的描边/轮廓仍残留，需即时刷新；
- 多行内容便签普通显示前 6 行，剩余进入快速编辑时再展开；
- 一行内容超过列宽应自动换行显示，不要省略。

**实现（modules/todo_notes.py）**
- **复选框 shift/ctrl + 行选择联动**：`_TodoItemDelegate` 新增 `editorEvent` 拦截 `COL_CHECK` 鼠标释放，转发给闭包 `_on_check_click(row, ctrl, shift)`：普通=切换当前行并更新锚点；ctrl=仅切换当前行（保留其余）；shift=按锚点行状态填充 `anchor..current` 区间。改动行同步持久化 `done` 到 DB，并用 `QItemSelection` 联动选中受影响区间（保证批复制对齐）。`blockSignals` 防止 `itemChanged` 回灌。
- **内容折行换行（前 6 行，不省略）**：内容 item 改为存放完整内容（不再 `[:80]` 截断，tooltip 保留全文）；delegate 新增 `_wrap_lines`（按列宽用 `QFontMetrics.horizontalAdvance` 逐字折行）、`sizeHint`（返回折行后高度，上限 `CONTENT_MAX_LINES=6` 行）、`paint`（用 `Qt.TextWordWrap` 绘制多行文本，替代默认省略号）。`refresh()` 中按折行行数设置每行行高（封顶 6 行）；进入编辑时 `_expand_row_for_content` 改为按完整折行高度展开全行。
- **删除内容后描边残留 → 即时刷新**：`setModelData` 内容分支提交后立即 `table.viewport().update()` 清除残留 ghost，配合既有 `request_refresh()` 全量重建。
- **日期主题彻底一致**：`_apply_date_theme` 重写——除 QSS（含 `QTableView`/`QHeaderView` 具体类型选择器）外，新增递归 `_paint()` 逐控件设置 `QPalette`（Window/Base/Text/WindowText/ButtonText/HighlightedText/PlaceholderText），兜底 QSS 渲染不到的内部件（暗色 #1e1e1e 底 + #e6e6e6 字）。offscreen 暗色下验证日历网格/星期表头/年选择器均浅字深底。

**实现（MCP 重启，core/tray.py + mcp_server.py）**
- `mcp_server.py` 新增 `app_restart(delay_seconds=2)` 工具：写 `{"command":"restart", ...}` 到 `DATA_DIR/mcp_inbox`，注册进 `TOOLS`（`app_restart`）。
- `core/tray.py` 的 mcp_inbox `_poll`：识别 `command=="restart"` 后 `_schedule_restart`（含延迟）→ `_do_restart` 调 `core.restart.restart_app()`（主线程，QTimer.singleShot 防重入）。

**测试**
- 新增 `tests/test_todo_notes_ui.py` 2 项：复选框 shift/ctrl 处理器（勾选/区间/ctrl 单切/行选择联动/DB 持久化）；内容完整保留 + 行高封顶 6 行。
- 全量 `pytest tests/ -q` **150 项通过**；smoke_test SMOKE OK；改动文件 py_compile 通过。
- 说明：MCP `app_restart` 需进程加载新代码后方可调用；当前运行进程（main.py、mcp_server stdio）内存中仍是旧代码，需重启后该工具才可用。


---

## 2026-09-03（便签模块：行高编辑后恢复到多行显示（修复单行塌陷））

**需求（来自 todo_notes id 3 更新，主诉求 1）**
- 条目选择/编辑后行高瞬间恢复到单行 —— 修复该问题（行内编辑结束后行高应恢复到 <=6 行的折行显示，而不是塌陷到默认单行 30）。

**实现（modules/todo_notes.py）**
- 新增 `_restore_content_row_height(index)`：内容列编辑结束后（`destroyEditor`）按该条目内容的折行行数恢复行高（封顶 `CONTENT_MAX_LINES=6` 行），替代原来恢复为 `defaultSectionSize()`（固定单行 30）的写法。
- 表现：多行内容便签编辑/选择后保持多行展开显示（<=6 行）；单行内容仍为一行。与「多行默认显示 <=6 行、进入快速编辑时展开」的要求保持一致。
- 其余项（日期主题与主程序一致、删除内容后即时刷新清除描边/轮廓、多行默认显示前 6 行快速编辑再展开）上一轮已实现并生效，本轮功能脚本复核通过：删除内容提交为空值 + 延迟 refresh 重建清除 ghost；日期主题递归 QPalette 暗色下浅字深底。

**测试**
- 新增 `tests/test_todo_notes_ui.py` 1 项：`test_destroy_editor_restores_multiline_not_single`（编辑结束 destroyEditor 后行高恢复为 <=6 行折行显示，而非单行）；同步更新旧用例 `test_content_inline_edit_uses_multiline_and_restores_row` 以反映新（正确）行为。
- 全量 `pytest tests/ -q` **151 项通过**；smoke_test SMOKE OK；改动文件 py_compile 通过；DB 无污染（残留行已清理）。
- 通过 MCP `app_restart` 工具重启程序应用修改成功：调用运行中 GUI 的 HTTP MCP 接口 `tools/call app_restart`（写 mcp_inbox/delay 2s），tray 轮询消费命令并重启新进程（端口 8765 由新实例接管），日志无 ERROR。id 3 标记 done。


---

## 2026-09-03（便签模块：初次打开单行被撑成 6 行 / 滚动残留描边修复）

**需求（来自 todo_notes id 3 更新）**
1. 初次打开便签模块每行都按 6 行显示（哪怕只有单行），点击后才自适应到具体行数 —— 修复。多行内容默认显示所有内容但最大不超过 6 行，多于 6 行在进入快速编辑时再展开。
2. 日期选择主题是黑色和字体混在一起 —— 与主程序保持一致。
3. 滚动多行内容时文字的描边/轮廓残留 —— 即时刷新。

**实现（modules/todo_notes.py）**
- **（1）首次打开全 6 行根因**：`refresh()` 在建页时同步调用，此时内容列宽仍是自适应前的默认窄宽，`_wrap_lines` 把本可单行显示的内容按窄列宽折行成 >=6 行，命中 `CONTENT_MAX_LINES` 上限，于是所有行都显示 6 行；待自适应 reflow 落定真实列宽后没有再重算行高，直到点击才纠正。
- **修复方案**：
  - 新增 `_fit_content_heights()`：按当前内容列宽统一重算每行折行显示高度（<=6 行），`refresh()` 填充完数据后调用；
  - 挂接 `horizontalHeader().sectionResized`（内容列）→ `_fit_content_heights()`：自适应 reflow 首次落定列宽、窗口缩放、用户拖拽内容列宽时都会按最新列宽重算行高，彻底消除「先 6 行、点击才自适应」；`_editing` 时跳过以免干扰行内编辑。
- **（3）滚动残留**：透明表格在滚动多行内容时 Qt 部分重绘会残留旧文字描边/轮廓；挂接 `verticalScrollBar`/`horizontalScrollBar` 的 `valueChanged` → `table.viewport().update()`，滚动即整块刷新视口，即时清除残留。
- **（2）日期主题**：上一轮 `_apply_date_theme`（QSS + 递归 QPalette）已生效；本轮暗色探针复核：日历日视图/星期表头/导航按钮/年选择器均为浅字（#e6e6e6）深底（#1e1e1e/#232323/#2b2b2b），不再黑底黑字混在一起。

**测试**
- 新增 `tests/test_todo_notes_ui.py` 1 项：`test_single_line_rows_not_forced_to_six_lines`（单行内容不被撑成 6 行；内容列宽 60->700 变化后单行仍为单行、多行收缩到 <=6 行）。
- 全量 `pytest tests/ -q` **152 项通过**；smoke_test SMOKE OK；改动文件 py_compile 通过；DB 无污染（残留行已清理）。
- 通过 MCP HTTP `tools/call app_restart` 重启程序应用修改成功：tray 消费 mcp_inbox 命令并重启新进程（端口 8765 由新实例接管），日志无 ERROR。id 3 标记 done。


---

## 2026-09-03（便签模块：快速编辑白字泄漏 / 内容列限宽防窄列换行 / 日期主题强化）

**需求（来自 todo_notes id 3 更新）**
1. 日期选择主题是黑色和字体完全混在一起 —— 与主程序主题一致。
2. 进入快速编辑模式时整体字符向右下角移动一点，同时背后白色字体漏出来。
3. 与 2 类似：背后漏出的白色字体对应的文字是没有修改的原文字。
4. 优化列宽计算，尽量让窄的那些列的内容别换行。

**实现（modules/todo_notes.py + ui/adaptive_table.py）**
- **（2）（3）快速编辑白字泄漏 / 偏移**：根因是全局主题把 `QLineEdit`/`QPlainTextEdit`/`QComboBox` 设为半透明背景（`rgba(...,0.85)`）并带边框；行内编辑时半透明编辑器叠在单元格上，底层单元格被选中的**原文字（白字）**从编辑器透出，且编辑器自身内边距让文字相对错位。修复：delegate 新增 `_editing_cell = (row, col)`，`_do_inline_edit` 进入编辑时记录该单元格并 `viewport().update()`；`paint` 中对正在编辑的单元格只画背景/高亮、**不画原文字**（彻底阻止白字/描边透出）；`destroyEditor` 结束编辑时清空 `_editing_cell` 使其恢复重画新文字。
- **（4）窄列内容别换行**：根因是内容列按**原始全文**以单行测宽（`adaptive_table._content_width` 用 `item.text()` 全文算宽度），内容列因此吃满窗口、挤压标题/创建时间等窄列导致其内容被截断/换行。修复：`make_adaptive_table` 新增可选 `width_caps={列: 该列最多占视口宽的比例}`，在 `_content_width` 中按比例上限裁剪；todo_notes 对内容列传 `width_caps={COL_CONTENT: 0.5}`（最多占一半视口、下限 250）。验证：修正后标题列 46→98（需要 84）、创建时间列 55→119（需要 101），各窄列均 ≥ 内容单行宽（ALL_NARROW_FIT=True），内容列降至 436px 不再挤占。
- **（1）日期主题强化**：`_apply_date_theme` 原有 QSS + 递归 QPalette 已生效（暗色下日历日视图/星期表头/导航/年选择器均为浅字深底）；本次进一步把应用逻辑提取为 `_apply()` 并在每次日历弹出（`QCalendarWidget` 的 `QEvent.Show`）时经事件过滤器重新应用，兜底内部视图首次弹出才创建而漏主题的场景。

**测试**
- 新增 `tests/test_todo_notes_ui.py` 1 项：`test_content_col_cap_keeps_narrow_columns_fit`（内容列限宽后，各非内容列宽度 ≥ 其内容单行宽，内容列 ≤ 视口宽）。
- 全量 `pytest tests/ -q` **153 项通过**；smoke_test SMOKE OK；改动文件 py_compile 通过；DB 无污染（残留行已清理）。
- 通过 MCP HTTP `tools/call app_restart` 重启程序应用修改成功：tray 消费 mcp_inbox 命令并重启新进程（端口 8765 由新实例 PID 12604 接管），日志无 ERROR。id 3 标记 done。
---

## 2026-09-03（便签模块：快速编辑不高亮被编辑行 / 全选移到复选框列表头）

**需求（来自 todo_notes id 3 更新，本轮三项收尾）**
1. 进入快速编辑时不高亮被编辑的那一行——修复。
2. 把「全选」从工具栏移到复选框列（首列）的列标题栏，点击复选框即勾选/取消全部——实现。
3. 仅编辑时全部展开多行：已有 `_expand_row_for_content` 于 `_do_inline_edit` 中展开完整内容，普通显示封顶 `CONTENT_MAX_LINES=6`，本轮确认不再改动。

**实现（modules/todo_notes.py）**
- **（1）不高亮被编辑行**：`_do_inline_edit` 进入行内编辑前调用 `table.clearSelection()`，清空行选择，避免被编辑行被蓝/灰高亮（编辑中的单元格由 delegate `_editing_cell` 仅画背景、不画原文字，原有逻辑保留）。
- **（2）全选改到头表复选框列**：
  - 新增 `_SelectAllHeader(QtWidgets.QHeaderView)`：在首列（`COL_CHECK`）标题位置绘制一个全选复选框（`paintSection` 用 `PE_IndicatorCheckBox`），点击该格触发 `_toggled(bool)` 信号；提供 `set_all_checked(bool)` 同步勾选态并重绘。
  - `_make_page_widget` 用 `table.setHorizontalHeader(_sel_header)` 替换默认表头；原工具栏「全选」复选框移除；`_on_select_all_toggled` 全表勾选/取消，`_update_select_all_state` 改为调用 `_sel_header.set_all_checked`，全选时列表头即时反馈（与单行复选框点击/refresh 联动）。

**测试**
- 新增 `tests/test_todo_notes_ui.py` 2 项：`test_select_all_lives_in_checkbox_header`（全选交互位于列表头复选框列而非工具栏）、`test_select_all_header_state_syncs_after_refresh`（refresh 后列表头全选态同步）。增强事件耗损计数以提升跨用例稳定性。
- 修复既有脆弱用例：`test_content_col_cap_keeps_narrow_columns_fit` 改为使用足够宽的独立窗口（1400px），使「各窄列宽度 ≥ 其内容单行宽」属性在所设视口下可实际成立，避免 8 列布局在 820px 视口下被等比压缩到低于内容宽导致的偶发失败；运行前后清理自身写入的测试条目，不再依赖共享 DB 的残留行状态。
- 全量 `pytest tests/ -q` **145 项通过**（多次运行稳定）；smoke_test SMOKE OK；改动文件 py_compile 通过；DB 无污染（残留行已清理，仅剩真实记录 id 2/3/5/22）。
- 通过 MCP `app_restart` 重启程序应用修改。id 3 标记 done。

---

## 2026-09-04（主页卡片拖拽/缩放崩溃修复 + RSS 性能 + 崩溃兜底）

### 一、主页卡片拖拽/缩放偶发崩溃修复（核心，`ui/home_tab.py`）

**现象**：拖动/调整大小的过程中随机闪退（原生段错误，Python 异常钩子抓不到）。

**根因**：
- `_relayout_all` 每次调用都会为**所有**位置变化的 proxy 新建一个 `QPropertyAnimation(proxy, b"pos")` 并 `start()`，且不保存、不回收。拖动过程中 `_relayout_all` 高频触发 → 同一 proxy 上叠加无数动画，互相抢占目标位置。
- 卡片被移除/重建时旧动画仍盯着已销毁的 proxy → 动画回调已删除的 C++ 对象 → 段错误。

**修复（`ui/home_tab.py`）**：
1. **动画管理字典** `self._animations = {}`（cid → 动画），每张卡片同一时刻只保留一个动画；新建前先 `_stop_animation(cid)` 停掉旧的。
2. **`_stop_animation(cid)`**：从字典取出动画 → `stop()`（`RuntimeError` 兜底）→ `setTargetObject(None)` 解除对已删 proxy 的引用后再移除。
3. **正在拖拽/缩放的卡片不参与动画**：`_dragging_cid` 标记当前被鼠标操作的卡片，`_relayout_all` 中该卡片直接 `setPos` 落地，避免动画与拖拽互相打架；拖拽结束 / 手柄松开时清除标记。新增 `_cid_of(proxy)` 查询与 `_mark_dragging()` 辅助。
4. **`_Proxy`/`_Handle` 加 `hasattr` 防御**：测试用的最小 `FakeDragOwner` 缺少 `_cid_of`/`_dragging_cid` 也能正常跑。
5. `_render_all`/`_remove_card` 也先 `_stop_animation(cid)` 再移除 proxy。

### 二、原生崩溃兜底：faulthandler（`main.py`）

- 启动早期启用 `faulthandler.enable(file=open(LOG_DIR/crash_faulthandler.log, "a"))`，原生层段错误时把 C 栈写到 `data/logs/crash_faulthandler.log`，便于下次定位 Qt/PySide6 层面的闪退。
- 复用 `core.logger.LOG_DIR`；try/except 兜底不影响正常启动。

### 三、RSS：WebEngine 预览惰性化 + 打开提速（`modules/rss_aggregator.py`）

- **WebEngine 惰性创建**：原来 `_RssPageWidget` 构造时就 `_make_preview_view()` 初始化整个 WebEngine profiile/view，拖慢 RSS 页打开。改为 `_ensure_preview_web()` 首次真正展示预览时才构造；不可用时回退 `QTextBrowser`；预览栈结构改为 `_summary_panel` + 惰性 web view。
- `_make_preview_view` 改用 `QWebEngineProfile("", parent)`（非持久化、不留缓存/cookie）。
- 侧边栏样式选择器 `QTreeWidget` → `QListWidget`（实际控件类型物配）。
- 打开 RSS 页关键路径加 `perf.timed` 埋点（sidebar_reload / load_items / webengine / favicon_check），便于抓打开耗时热点。

### 四、RSS 存储层性能优化（`modules/rss_store.py`）

1. **每线程连接复用 + 连接注册表** `_conn_registry`：同线程多次调用复用同一连接，避免频繁 `sqlite3.connect`；空闲线程连接自动剪枝（>32 或线程退出），`_close_conn` 显式释放。
2. **Schema 快速路径**：`_init_schema` 先读 `PRAGMA user_version`，等于 `_SCHEMA_VERSION(3)` 直接跳过整段幂等迁移（大量 `PRAGMA table_info`），仅首次/旧库才跑迁移并写版本号。
3. **批量查询消 N+1**：
   - `list_sidebar`：未读数与标签改为一次 `GROUP BY` 全部取出（原先每个 feed/聚合各跑一条查询）。
   - `list_feeds`：标签一次 join 取出。
4. **`list_tags` / `get_keywords` 结果缓存**：加失效点（入库后 / 增删关键词后 `invalidate_*`），避免重复全表 `SELECT DISTINCT`。
5. 关键查询加 `@trace()` 埋点（list_feeds/list_tags/get_item/search/recent/list_sidebar/check_keywords）。

### 五、MCP：todo 表预建（`mcp_server.py`）

- 各 todo 工具前加 `_ensure_todo_table(conn)`：`CREATE TABLE IF NOT EXISTS todo_notes`，避免对全新/被清理的库直接查询报 `no such table`。

### 六、测试加固（`tests/test_todo_notes_ui.py`）

- 新增 `_ensure_test_data()` / `_cleanup_test_data()` 测试数据辅助，部分依赖共享 DB 的用例先自建 `__test_todo__` 行，减少对遗留记录的耦合。

### 七、回归与验证

- 全量 `pytest tests/ -v` **155 passed**（覆盖主页拖拽/缩放、RSS、MCP、todo 等）。
- 离屏自动化拖动/缩放复跑（拖动、缩放 r/b/c 手柄、动画期间移除/重置/清空）均无崩溃。
- 附：本轮同时提交 `opencode.json`（MCP 客户端配置）；`.opencode/`（含 node_modules）与 `YZplan.lnk`（本机快捷方式）加入 `.gitignore` 不入库。
