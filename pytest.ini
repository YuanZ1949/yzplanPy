[pytest]
testpaths = tests
addopts = -q
# Collect only test_*.py. smoke_test.py is a standalone smoke script
# (python tests/smoke_test.py); excluding it from pytest collection avoids
# collection-time side effects (global app font, booting the full app against
# the production DB) polluting the whole test session.
python_files = test_*.py